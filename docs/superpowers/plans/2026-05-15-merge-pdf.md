# PDF Merge Tool Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a browser-based PDF merge tool to OmniKit that lets users upload multiple PDFs, see thumbnail previews, reorder via drag-and-drop, exclude individual files, and merge into a single PDF for download.

**Architecture:** Business logic lives in `libs/pdf-merge/` — `merge.ts` for pdf-lib merge operations, `thumbnail.ts` for pdfjs-dist first-page rendering, `types.ts` for shared types. UI is a single client component `pdf-merge-page.tsx` using HTML5 Drag and Drop for reordering and `file-selector`'s `fromEvent` for file drops. pdfjs-dist is dynamically imported on first upload to keep the initial bundle lean.

**Tech Stack:** pdf-lib (static import), pdfjs-dist v4.x (dynamic import), file-selector (drag-and-drop), lucide-react icons, Tailwind CSS 4

---

## File Structure

### New Files

| File                                        | Responsibility                                              |
| ------------------------------------------- | ----------------------------------------------------------- |
| `libs/pdf-merge/types.ts`                   | `PdfFileEntry` and `MergeProgress` type definitions         |
| `libs/pdf-merge/merge.ts`                   | `mergePdfs()` and `getPdfPageCount()` — pdf-lib merge logic |
| `libs/pdf-merge/thumbnail.ts`               | `renderThumbnail()` — pdfjs-dist v4 first-page thumbnail    |
| `libs/pdf-merge/__tests__/merge.test.ts`    | Unit tests for merge logic                                  |
| `app/[locale]/pdf-merge/page.tsx`           | Route entry — SEO metadata + JSON-LD                        |
| `app/[locale]/pdf-merge/pdf-merge-page.tsx` | Client component — full UI + interaction logic              |
| `public/locales/en/pdf-merge.json`          | English UI strings + descriptions                           |
| `public/locales/zh-CN/pdf-merge.json`       | Simplified Chinese UI strings                               |
| `public/locales/zh-TW/pdf-merge.json`       | Traditional Chinese UI strings                              |
| `public/locales/ja/pdf-merge.json`          | Japanese UI strings                                         |
| `public/locales/ko/pdf-merge.json`          | Korean UI strings                                           |
| `public/locales/es/pdf-merge.json`          | Spanish UI strings                                          |
| `public/locales/pt-BR/pdf-merge.json`       | Portuguese (BR) UI strings                                  |
| `public/locales/fr/pdf-merge.json`          | French UI strings                                           |
| `public/locales/de/pdf-merge.json`          | German UI strings                                           |
| `public/locales/ru/pdf-merge.json`          | Russian UI strings                                          |

### Modified Files

| File                                            | Change                                                                      |
| ----------------------------------------------- | --------------------------------------------------------------------------- |
| `package.json`                                  | Add `pdf-lib`, `pdfjs-dist` deps; add `copy-pdf-worker` script              |
| `next.config.js`                                | Add `transpilePackages: ['pdfjs-dist']`, canvas alias                       |
| `libs/tools.ts`                                 | Add `pdf-merge` to TOOLS, TOOL_CATEGORIES, TOOL_RELATIONS; import FileStack |
| `vitest.config.ts`                              | Add `libs/pdf-merge/**/*.test.ts` to include array                          |
| `libs/__tests__/tool-relations.test.ts`         | Fix missing `expect(errors)` assertion                                      |
| `public/locales/{locale}/tools.json` (×10)      | Add `pdf-merge` entry                                                       |
| `public/locales/{locale}/categories.json` (×10) | Update `visual` category                                                    |

---

## Task 1: Install Dependencies and Configure Build

**Files:**

- Modify: `package.json`
- Modify: `next.config.js`

- [ ] **Step 1: Install pdf-lib and pdfjs-dist**

Run:

```bash
npm install pdf-lib pdfjs-dist@^4.9.0
```

- [ ] **Step 2: Add copy-pdf-worker script to package.json**

In `package.json`, add to the `scripts` object (before `"dev"`):

```json
"copy-pdf-worker": "cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs public/pdf.worker.min.mjs",
```

Then add `predev` and `prebuild` hooks:

```json
"predev": "npm run copy-pdf-worker",
"prebuild": "npm run copy-pdf-worker",
```

The final scripts section should look like:

```json
"scripts": {
  "copy-pdf-worker": "cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs public/pdf.worker.min.mjs",
  "predev": "npm run copy-pdf-worker",
  "prebuild": "npm run copy-pdf-worker",
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "prepare": "husky",
  "icons:generate": "node scripts/generate-pwa-icons.mjs",
  "typecheck:sw": "tsc -p tsconfig.sw.json",
  "test": "vitest run",
  "test:watch": "vitest",
  "test:a11y": "vitest run --config vitest.config.a11y.ts"
}
```

- [ ] **Step 3: Copy the pdfjs worker file to public/**

Run:

```bash
npm run copy-pdf-worker
```

- [ ] **Step 4: Update next.config.js for pdfjs-dist**

In `next.config.js`, add `transpilePackages` and canvas alias. Modify the `nextConfig` object:

```javascript
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ["pdfjs-dist"],
  // ... existing headers() ...
  turbopack: {
    resolveAlias: {
      fs: { browser: "./scripts/empty-module.js" },
      path: { browser: "./scripts/empty-module.js" },
      canvas: false,
    },
  },
  webpack: (config, { isServer }) => {
    if (!isServer) {
      config.resolve.fallback = {
        ...config.resolve.fallback,
        fs: false,
        path: false,
      };
    }
    config.resolve.alias.canvas = false;
    return config;
  },
};
```

Key changes:

- Add `transpilePackages: ["pdfjs-dist"]` at top level
- Add `canvas: false` to `turbopack.resolveAlias`
- Add `config.resolve.alias.canvas = false;` to webpack config

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json next.config.js public/pdf.worker.min.mjs
git commit -m "chore: add pdf-lib and pdfjs-dist dependencies with build config"
```

---

## Task 2: Create Types

**Files:**

- Create: `libs/pdf-merge/types.ts`

- [ ] **Step 1: Create the types file**

Create `libs/pdf-merge/types.ts`:

```typescript
export interface PdfFileEntry {
  id: string;
  file: File;
  name: string;
  size: number;
  pageCount: number;
  thumbnailUrl: string | null;
  arrayBuffer: ArrayBuffer;
  enabled: boolean;
}

export interface MergeProgress {
  current: number;
  total: number;
}
```

- [ ] **Step 2: Commit**

```bash
git add libs/pdf-merge/types.ts
git commit -m "feat(pdf-merge): add PdfFileEntry and MergeProgress types"
```

---

## Task 3: Implement Merge Logic

**Files:**

- Create: `libs/pdf-merge/merge.ts`

- [ ] **Step 1: Create the merge module**

Create `libs/pdf-merge/merge.ts`:

```typescript
import { PDFDocument } from "pdf-lib";

export async function getPdfPageCount(data: ArrayBuffer): Promise<number> {
  const doc = await PDFDocument.load(data, { ignoreEncryption: true });
  const count = doc.getPageCount();
  doc.destroy();
  return count;
}

export async function mergePdfs(
  files: ArrayBuffer[],
  onProgress?: (current: number, total: number) => void
): Promise<Uint8Array> {
  const merged = await PDFDocument.create();
  const total = files.length;

  for (let i = 0; i < files.length; i++) {
    onProgress?.(i + 1, total);
    const src = await PDFDocument.load(files[i], { ignoreEncryption: true });
    const pages = await merged.copyPages(src, src.getPageIndices());
    for (const page of pages) {
      merged.addPage(page);
    }
  }

  return merged.save();
}
```

Notes:

- `ignoreEncryption: true` allows loading PDFs that claim to be encrypted but have empty passwords (common for print-restricted PDFs). Truly encrypted PDFs will still fail at the content level.
- `getPdfPageCount` loads the full document (pdf-lib has no header-only parse mode) then destroys it.
- `mergePdfs` iterates input files in order, copies all pages from each via `copyPages` + `addPage`, calls `onProgress` once per file.

- [ ] **Step 2: Commit**

```bash
git add libs/pdf-merge/merge.ts
git commit -m "feat(pdf-merge): implement mergePdfs and getPdfPageCount"
```

---

## Task 4: Implement Thumbnail Rendering

**Files:**

- Create: `libs/pdf-merge/thumbnail.ts`

- [ ] **Step 1: Create the thumbnail module**

Create `libs/pdf-merge/thumbnail.ts`:

```typescript
export async function renderThumbnail(
  data: ArrayBuffer,
  maxWidth = 120,
  maxHeight = 160
): Promise<string> {
  const pdfjs = await import("pdfjs-dist");
  pdfjs.GlobalWorkerOptions.workerSrc = "/pdf.worker.min.mjs";

  const pdf = await pdfjs.getDocument({ data: new Uint8Array(data) }).promise;
  const page = await pdf.getPage(1);

  const viewport = page.getViewport({ scale: 1 });
  const scale = Math.min(maxWidth / viewport.width, maxHeight / viewport.height);
  const scaledViewport = page.getViewport({ scale });

  const canvas = document.createElement("canvas");
  canvas.width = scaledViewport.width;
  canvas.height = scaledViewport.height;

  const ctx = canvas.getContext("2d")!;
  await page.render({ canvasContext: ctx, viewport: scaledViewport }).promise;

  const dataUrl = canvas.toDataURL("image/png");
  pdf.destroy();
  return dataUrl;
}
```

Notes:

- Dynamic import of pdfjs-dist ensures it's only loaded when user uploads a file.
- Worker source is set each time before rendering (idempotent, avoids race conditions on first load).
- Renders first page at constrained dimensions (120×160px max) to off-screen canvas.
- Returns data URL string (not object URL — data URLs are stable and don't need revocation).

- [ ] **Step 2: Commit**

```bash
git add libs/pdf-merge/thumbnail.ts
git commit -m "feat(pdf-merge): implement renderThumbnail with pdfjs-dist v4"
```

---

## Task 5: Write Unit Tests

**Files:**

- Create: `libs/pdf-merge/__tests__/merge.test.ts`
- Modify: `vitest.config.ts`

- [ ] **Step 1: Add test scope to vitest.config.ts**

In `vitest.config.ts`, add to the `include` array (after the `libs/batch/**/*.test.ts` entry):

```typescript
"libs/pdf-merge/**/*.test.ts",
```

- [ ] **Step 2: Create the test file**

Create `libs/pdf-merge/__tests__/merge.test.ts`:

```typescript
import { describe, it, expect, vi } from "vitest";
import { PDFDocument } from "pdf-lib";
import { mergePdfs, getPdfPageCount } from "../merge";

async function makePdf(pageCount: number): Promise<ArrayBuffer> {
  const doc = await PDFDocument.create();
  for (let i = 0; i < pageCount; i++) {
    doc.addPage();
  }
  const bytes = await doc.save();
  return bytes.buffer as ArrayBuffer;
}

describe("mergePdfs", () => {
  it("merges two PDFs into one with combined page count", async () => {
    const pdf1 = await makePdf(3);
    const pdf2 = await makePdf(5);
    const result = await mergePdfs([pdf1, pdf2]);
    const merged = await PDFDocument.load(result);
    expect(merged.getPageCount()).toBe(8);
  });

  it("merges five PDFs correctly", async () => {
    const pdfs = await Promise.all([makePdf(1), makePdf(2), makePdf(3), makePdf(4), makePdf(5)]);
    const result = await mergePdfs(pdfs);
    const merged = await PDFDocument.load(result);
    expect(merged.getPageCount()).toBe(15);
  });

  it("handles single file merge", async () => {
    const pdf = await makePdf(7);
    const result = await mergePdfs([pdf]);
    const merged = await PDFDocument.load(result);
    expect(merged.getPageCount()).toBe(7);
  });

  it("returns empty PDF for empty array", async () => {
    const result = await mergePdfs([]);
    const merged = await PDFDocument.load(result);
    expect(merged.getPageCount()).toBe(0);
  });

  it("preserves input file order", async () => {
    const pdf1 = await makePdf(2);
    const pdf2 = await makePdf(3);
    const pdf3 = await makePdf(1);
    const result = await mergePdfs([pdf1, pdf2, pdf3]);
    const merged = await PDFDocument.load(result);
    expect(merged.getPageCount()).toBe(6);
  });

  it("calls onProgress once per file with correct indices", async () => {
    const pdfs = [await makePdf(1), await makePdf(1), await makePdf(1)];
    const progress = vi.fn();
    await mergePdfs(pdfs, progress);
    expect(progress).toHaveBeenCalledTimes(3);
    expect(progress).toHaveBeenCalledWith(1, 3);
    expect(progress).toHaveBeenCalledWith(2, 3);
    expect(progress).toHaveBeenCalledWith(3, 3);
  });

  it("throws on corrupted PDF data", async () => {
    const corrupted = new ArrayBuffer(10);
    await expect(mergePdfs([corrupted])).rejects.toThrow();
  });
});

describe("getPdfPageCount", () => {
  it("returns correct count for single-page PDF", async () => {
    const pdf = await makePdf(1);
    expect(await getPdfPageCount(pdf)).toBe(1);
  });

  it("returns correct count for multi-page PDF", async () => {
    const pdf = await makePdf(42);
    expect(await getPdfPageCount(pdf)).toBe(42);
  });

  it("returns 0 for empty PDF", async () => {
    const pdf = await makePdf(0);
    expect(await getPdfPageCount(pdf)).toBe(0);
  });

  it("throws on corrupted data", async () => {
    const corrupted = new ArrayBuffer(10);
    await expect(getPdfPageCount(corrupted)).rejects.toThrow();
  });
});
```

- [ ] **Step 3: Run tests to verify they pass**

Run:

```bash
npm test -- libs/pdf-merge
```

Expected: All tests PASS.

- [ ] **Step 4: Commit**

```bash
git add vitest.config.ts libs/pdf-merge/__tests__/merge.test.ts
git commit -m "test(pdf-merge): add unit tests for merge logic and page counting"
```

---

## Task 6: Register Tool in libs/tools.ts

**Files:**

- Modify: `libs/tools.ts`
- Modify: `libs/__tests__/tool-relations.test.ts`

- [ ] **Step 1: Import FileStack icon**

In `libs/tools.ts`, add `FileStack` to the lucide-react import block (line 3-45):

```typescript
import {
  // ... existing imports ...
  Layers,
  FileStack,
} from "lucide-react";
```

- [ ] **Step 2: Add pdf-merge to TOOLS array**

Add after the `batch` entry (the last entry in the TOOLS array, before the closing `]`):

```typescript
  {
    key: "pdf-merge",
    path: "/pdf-merge",
    icon: FileStack,
    emoji: "📑",
    sameAs: [
      "https://en.wikipedia.org/wiki/PDF",
      "https://developer.mozilla.org/en-US/docs/Glossary/PDF",
    ],
  },
```

- [ ] **Step 3: Add pdf-merge to visual category in TOOL_CATEGORIES**

Change line 122 from:

```typescript
{ key: "visual", tools: ["color", "image-resize", "image-compress", "image-convert"] },
```

to:

```typescript
{ key: "visual", tools: ["color", "image-resize", "image-compress", "image-convert", "pdf-merge"] },
```

- [ ] **Step 4: Add TOOL_RELATIONS entry and update reverse relations**

Add `pdf-merge` relations (must be 2–5, bidirectional symmetric):

```typescript
"pdf-merge": ["image-compress", "image-convert", "checksum"],
```

Update existing entries for bidirectional symmetry:

- `"image-compress"`: change from `["image-resize", "image-convert", "checksum"]` to `["image-resize", "image-convert", "checksum", "pdf-merge"]` (now 4 relations)
- `"image-convert"`: change from `["image-resize", "image-compress", "qrcode"]` to `["image-resize", "image-compress", "qrcode", "pdf-merge"]` (now 4 relations)
- `"checksum"`: change from `["hashing", "cipher"]` to `["hashing", "cipher", "pdf-merge"]` (now 3 relations)

- [ ] **Step 5: Fix tool-relations test bug**

In `libs/__tests__/tool-relations.test.ts`, the bidirectional test (line 42-51) has a missing assertion. Add at line 51:

```typescript
it("relations are bidirectional (if A lists B, B should list A)", () => {
  const errors: string[] = [];
  for (const key of allKeys) {
    for (const rel of TOOL_RELATIONS[key]) {
      if (!TOOL_RELATIONS[rel]?.includes(key)) {
        errors.push(`${key}→${rel} (reverse missing)`);
      }
    }
  }
  expect(errors, `Bidirectional errors: ${errors.join(", ")}`).toEqual([]);
});
```

- [ ] **Step 6: Run tool-relations test to verify**

Run:

```bash
npm test -- libs/__tests__/tool-relations.test.ts
```

Expected: All tests PASS (including the now-fixed bidirectional test).

- [ ] **Step 7: Commit**

```bash
git add libs/tools.ts libs/__tests__/tool-relations.test.ts
git commit -m "feat(pdf-merge): register tool in TOOLS, TOOL_CATEGORIES, and TOOL_RELATIONS"
```

---

## Task 7: Create English i18n Files

**Files:**

- Create: `public/locales/en/pdf-merge.json`
- Modify: `public/locales/en/tools.json`
- Modify: `public/locales/en/categories.json`

- [ ] **Step 1: Add pdf-merge entry to en/tools.json**

In `public/locales/en/tools.json`, add before the `"categories"` key:

```json
  "pdf-merge": {
    "title": "PDF Merger - Combine Multiple PDFs into One",
    "shortTitle": "PDF Merger",
    "description": "Merge multiple PDF files into a single document. Drag-and-drop reorder, thumbnail preview, and instant download. All processing runs in your browser."
  },
```

No `searchTerms` needed for English (fuzzysort matches shortTitle directly).

- [ ] **Step 2: Create en/pdf-merge.json**

Create `public/locales/en/pdf-merge.json`:

```json
{
  "dropPdf": "Drop PDF files here or click to select",
  "supportedFormats": "Supports PDF files only",
  "addMoreFiles": "Add more files",
  "mergeButton": "Merge & Download",
  "mergeButtonInfo": "{count} files / {pages} pages",
  "excludedCount": "{count} excluded",
  "merging": "Merging...",
  "mergeProgress": "Processing file {current} of {total}...",
  "mergeSuccess": "Merged successfully!",
  "mergedResult": "{name} — {pages} pages, {size}",
  "download": "Download",
  "newMerge": "New Merge",
  "onlyPdfSupported": "Only PDF files are supported",
  "encryptedPdf": "This PDF is encrypted and cannot be merged",
  "corruptedPdf": "This PDF file is corrupted and cannot be read",
  "mergeFailed": "Merge failed. Please try removing the problematic file.",
  "pages": "{count} pages",
  "descriptions": {
    "title": "About PDF Merger",
    "aeoDefinition": "PDF Merger is a free online tool for combining multiple PDF files into a single document. Drag-and-drop to reorder, preview thumbnails, and download the merged result. All processing runs locally in your browser.",
    "whatIsTitle": "What is the PDF Merger?",
    "whatIs": "Combine multiple PDF files into a single document directly in your browser. Upload your PDFs, drag to reorder, toggle individual files on or off, and download the merged result. No data is uploaded to any server — all processing uses the pdf-lib library.",
    "useCases": "When to use PDF Merger",
    "useCasesP1": "Combine multiple reports, invoices, or contracts into a single file for easy sharing.",
    "useCasesP2": "Merge scanned documents or printouts into one cohesive PDF.",
    "stepsTitle": "How to Merge PDFs",
    "step1Title": "Upload files",
    "step1Text": "Drag and drop your PDF files or click to select them from your computer.",
    "step2Title": "Arrange and configure",
    "step2Text": "Drag to reorder files. Toggle off any files you want to exclude from the merge.",
    "step3Title": "Merge and download",
    "step3Text": "Click the Merge button to combine all enabled files into a single PDF and download it.",
    "faq1Q": "Is there a file size limit?",
    "faq1A": "No. PDF merging runs entirely in your browser, so there is no server-side file size limit. Very large files may take longer to process.",
    "faq2Q": "Can I reorder pages within a PDF?",
    "faq2A": "No. This tool merges entire PDF files in the order you arrange them. Each PDF is treated as an atomic unit — all its pages stay together.",
    "faq3Q": "Are my files uploaded to a server?",
    "faq3A": "No. All processing happens locally in your browser using the pdf-lib library. Your files never leave your device."
  }
}
```

- [ ] **Step 3: Update en/categories.json visual section**

In `public/locales/en/categories.json`, replace the `"visual"` object with:

```json
  "visual": {
    "title": "Visual & Media Tools - Color Converter, Image Compressor, PDF Merger",
    "shortTitle": "Visual & Media",
    "description": "Free online visual and media tools for developers. Convert colors, compress and convert images, merge PDFs. 100% client-side.",
    "intro": "5 visual and media tools for design, image, and document workflows. The Color Converter handles 7 color spaces with palette extraction and contrast checking. Image Resizer, Compressor, and Converter handle image transformations. PDF Merger combines multiple PDFs into one with drag-and-drop reordering. All processing runs locally in your browser.",
    "faq1Q": "What color spaces are supported?",
    "faq1A": "The Color Converter supports HEX, RGB, HSL, HSV, CMYK, LAB, and OKLCH with bidirectional conversion and visual comparison.",
    "faq2Q": "Is there a file size limit for image compression?",
    "faq2A": "No. Image compression runs entirely in your browser using Canvas API, so there is no server-side file size limit. Very large images may be slower to process."
  },
```

Key changes: `title` adds "PDF Merger", `intro` updates count to 5 and adds PDF Merger description.

- [ ] **Step 4: Commit**

```bash
git add public/locales/en/pdf-merge.json public/locales/en/tools.json public/locales/en/categories.json
git commit -m "feat(pdf-merge): add English i18n strings and update categories"
```

---

## Task 8: Create CJK i18n Files (zh-CN, zh-TW, ja, ko)

**Files:**

- Create: `public/locales/zh-CN/pdf-merge.json`
- Create: `public/locales/zh-TW/pdf-merge.json`
- Create: `public/locales/ja/pdf-merge.json`
- Create: `public/locales/ko/pdf-merge.json`
- Modify: `public/locales/zh-CN/tools.json`, `public/locales/zh-TW/tools.json`, `public/locales/ja/tools.json`, `public/locales/ko/tools.json`
- Modify: `public/locales/zh-CN/categories.json`, `public/locales/zh-TW/categories.json`, `public/locales/ja/categories.json`, `public/locales/ko/categories.json`

- [ ] **Step 1: Add pdf-merge to zh-CN/tools.json**

Add before the `"categories"` key:

```json
  "pdf-merge": {
    "title": "PDF 合并工具 - 在线合并多个 PDF 文件",
    "shortTitle": "PDF 合并",
    "description": "将多个 PDF 文件合并为一个文档。支持拖拽排序、缩略图预览、即时下载。所有处理在浏览器本地完成。",
    "searchTerms": "pdfhebing pdfhb hebing pdf wenjian"
  },
```

- [ ] **Step 2: Create zh-CN/pdf-merge.json**

```json
{
  "dropPdf": "拖放 PDF 文件到此处，或点击选择",
  "supportedFormats": "仅支持 PDF 文件",
  "addMoreFiles": "添加更多文件",
  "mergeButton": "合并并下载",
  "mergeButtonInfo": "{count} 个文件 / {pages} 页",
  "excludedCount": "{count} 个已排除",
  "merging": "合并中...",
  "mergeProgress": "正在处理第 {current} 个文件（共 {total} 个）...",
  "mergeSuccess": "合并成功！",
  "mergedResult": "{name} — {pages} 页，{size}",
  "download": "下载",
  "newMerge": "重新合并",
  "onlyPdfSupported": "仅支持 PDF 文件",
  "encryptedPdf": "此 PDF 已加密，无法合并",
  "corruptedPdf": "此 PDF 文件已损坏，无法读取",
  "mergeFailed": "合并失败，请尝试移除有问题的文件。",
  "pages": "{count} 页",
  "descriptions": {
    "title": "关于 PDF 合并工具",
    "aeoDefinition": "PDF 合并工具是一款免费的在线工具，可将多个 PDF 文件合并为一个文档。支持拖拽排序、缩略图预览和下载合并结果。所有处理均在浏览器本地完成。",
    "whatIsTitle": "什么是 PDF 合并工具？",
    "whatIs": "在浏览器中将多个 PDF 文件合并为一个文档。上传 PDF 文件，拖拽调整顺序，开启或关闭单个文件，下载合并结果。不会上传任何数据到服务器——所有处理均使用 pdf-lib 库完成。",
    "useCases": "何时使用 PDF 合并工具",
    "useCasesP1": "将多份报告、发票或合同合并为一个文件，便于分享。",
    "useCasesP2": "将扫描文档或打印件合并为一个完整的 PDF。",
    "stepsTitle": "如何合并 PDF",
    "step1Title": "上传文件",
    "step1Text": "拖放 PDF 文件到上传区域，或点击从电脑中选择。",
    "step2Title": "排列和配置",
    "step2Text": "拖拽调整文件顺序。关闭不需要合并的文件。",
    "step3Title": "合并并下载",
    "step3Text": "点击合并按钮，将所有启用的文件合并为一个 PDF 并下载。",
    "faq1Q": "有文件大小限制吗？",
    "faq1A": "没有。PDF 合并完全在浏览器中运行，没有服务端文件大小限制。非常大的文件可能需要更长处理时间。",
    "faq2Q": "可以调整 PDF 内部的页面顺序吗？",
    "faq2A": "不可以。此工具按您排列的顺序合并整个 PDF 文件。每个 PDF 被视为一个整体——其所有页面保持在一起。",
    "faq3Q": "我的文件会上传到服务器吗？",
    "faq3A": "不会。所有处理均在浏览器中使用 pdf-lib 库本地完成。您的文件不会离开您的设备。"
  }
}
```

- [ ] **Step 3: Update zh-CN/categories.json visual section**

Replace the `"visual"` object:

```json
  "visual": {
    "title": "视觉媒体工具 - 颜色转换器、图片压缩、PDF 合并",
    "shortTitle": "视觉媒体",
    "description": "面向开发者的免费在线视觉媒体工具。颜色转换、图片压缩转换、PDF 合并。100% 浏览器端处理。",
    "intro": "5 个视觉媒体工具。颜色转换器支持 7 种色彩空间，具备取色器、图片调色板提取和对比度检查功能。图片缩放、压缩、格式转换工具处理图片变换。PDF 合并工具支持拖拽排序多个 PDF 文件。所有处理均在本地运行。",
    "faq1Q": "支持哪些色彩空间？",
    "faq1A": "颜色转换器支持 HEX、RGB、HSL、HSV、CMYK、LAB 和 OKLCH，支持双向转换和可视化对比。",
    "faq2Q": "图片压缩有文件大小限制吗？",
    "faq2A": "没有。图片压缩完全在浏览器中使用 Canvas API 处理，因此没有服务端文件大小限制。非常大的图片处理可能会稍慢。"
  },
```

- [ ] **Step 4: Add pdf-merge to zh-TW/tools.json**

```json
  "pdf-merge": {
    "title": "PDF 合併工具 - 線上合併多個 PDF 檔案",
    "shortTitle": "PDF 合併",
    "description": "將多個 PDF 檔案合併為一個文件。支援拖曳排序、縮圖預覽、即時下載。所有處理在瀏覽器本機完成。",
    "searchTerms": "pdfhebing pdfhb hebing pdf wenjian"
  },
```

- [ ] **Step 5: Create zh-TW/pdf-merge.json**

```json
{
  "dropPdf": "拖放 PDF 檔案到此处，或點選選擇",
  "supportedFormats": "僅支援 PDF 檔案",
  "addMoreFiles": "新增更多檔案",
  "mergeButton": "合併並下載",
  "mergeButtonInfo": "{count} 個檔案 / {pages} 頁",
  "excludedCount": "{count} 個已排除",
  "merging": "合併中...",
  "mergeProgress": "正在處理第 {current} 個檔案（共 {total} 個）...",
  "mergeSuccess": "合併成功！",
  "mergedResult": "{name} — {pages} 頁，{size}",
  "download": "下載",
  "newMerge": "重新合併",
  "onlyPdfSupported": "僅支援 PDF 檔案",
  "encryptedPdf": "此 PDF 已加密，無法合併",
  "corruptedPdf": "此 PDF 檔案已損毀，無法讀取",
  "mergeFailed": "合併失敗，請嘗試移除有問題的檔案。",
  "pages": "{count} 頁",
  "descriptions": {
    "title": "關於 PDF 合併工具",
    "aeoDefinition": "PDF 合併工具是一款免費的線上工具，可將多個 PDF 檔案合併為一個文件。支援拖曳排序、縮圖預覽和下載合併結果。所有處理均在瀏覽器本機完成。",
    "whatIsTitle": "什麼是 PDF 合併工具？",
    "whatIs": "在瀏覽器中將多個 PDF 檔案合併為一個文件。上傳 PDF 檔案，拖曳調整順序，開啟或關閉單一檔案，下載合併結果。不會上傳任何資料到伺服器——所有處理均使用 pdf-lib 函式庫完成。",
    "useCases": "何時使用 PDF 合併工具",
    "useCasesP1": "將多份報告、發票或合約合併為一個檔案，便於分享。",
    "useCasesP2": "將掃描文件或列印件合併為一個完整的 PDF。",
    "stepsTitle": "如何合併 PDF",
    "step1Title": "上傳檔案",
    "step1Text": "拖放 PDF 檔案到上傳區域，或點選從電腦中選擇。",
    "step2Title": "排列和設定",
    "step2Text": "拖曳調整檔案順序。關閉不需要合併的檔案。",
    "step3Title": "合併並下載",
    "step3Text": "點選合併按鈕，將所有啟用的檔案合併為一個 PDF 並下載。",
    "faq1Q": "有檔案大小限制嗎？",
    "faq1A": "沒有。PDF 合併完全在瀏覽器中執行，沒有伺服器端檔案大小限制。非常大的檔案可能需要更長處理時間。",
    "faq2Q": "可以調整 PDF 內部的頁面順序嗎？",
    "faq2A": "不可以。此工具按您排列的順序合併整個 PDF 檔案。每個 PDF 被視為一個整體——其所有頁面保持在一起。",
    "faq3Q": "我的檔案會上傳到伺服器嗎？",
    "faq3A": "不會。所有處理均在瀏覽器中使用 pdf-lib 函式庫本機完成。您的檔案不會離開您的裝置。"
  }
}
```

- [ ] **Step 6: Update zh-TW/categories.json visual section**

Update the `"visual"` object similarly to zh-CN, using Traditional Chinese characters.

- [ ] **Step 7: Add pdf-merge to ja/tools.json**

```json
  "pdf-merge": {
    "title": "PDF 結合ツール - 複数の PDF を1つに結合",
    "shortTitle": "PDF 結合",
    "description": "複数の PDF ファイルを1つのドキュメントに結合。ドラッグ＆ドロップで並べ替え、サムネイルプレビュー、即座にダウンロード。すべての処理はブラウザで実行されます。",
    "searchTerms": "pdfgattai pdfgt gattai pdf bunnsyo"
  },
```

- [ ] **Step 8: Create ja/pdf-merge.json**

```json
{
  "dropPdf": "PDF ファイルをここにドロップ、またはクリックして選択",
  "supportedFormats": "PDF ファイルのみ対応",
  "addMoreFiles": "ファイルを追加",
  "mergeButton": "結合してダウンロード",
  "mergeButtonInfo": "{count} ファイル / {pages} ページ",
  "excludedCount": "{count} 件除外",
  "merging": "結合中...",
  "mergeProgress": "{total} ファイル中 {current} ファイル目を処理中...",
  "mergeSuccess": "結合完了！",
  "mergedResult": "{name} — {pages} ページ、{size}",
  "download": "ダウンロード",
  "newMerge": "新しく結合",
  "onlyPdfSupported": "PDF ファイルのみ対応しています",
  "encryptedPdf": "この PDF は暗号化されており、結合できません",
  "corruptedPdf": "この PDF ファイルは破損しており、読み取れません",
  "mergeFailed": "結合に失敗しました。問題のあるファイルを削除してお試しください。",
  "pages": "{count} ページ",
  "descriptions": {
    "title": "PDF 結合ツールについて",
    "aeoDefinition": "PDF 結合ツールは、複数の PDF ファイルを1つのドキュメントに結合する無料のオンラインツールです。ドラッグ＆ドロップで並べ替え、サムネイルプレビュー、結合結果をダウンロード。すべての処理はブラウザ内でローカルに実行されます。",
    "whatIsTitle": "PDF 結合ツールとは？",
    "whatIs": "ブラウザ内で複数の PDF ファイルを1つのドキュメントに結合します。PDF をアップロードし、ドラッグで並べ替え、個別のファイルをオン/オフして、結合結果をダウンロードします。データはサーバーにアップロードされません — すべての処理は pdf-lib ライブラリを使用します。",
    "useCases": "PDF 結合ツールの活用場面",
    "useCasesP1": "複数のレポート、請求書、契約書を1つのファイルにまとめて共有に便利です。",
    "useCasesP2": "スキャンした文書や印刷物を1つの PDF にまとめます。",
    "stepsTitle": "PDF の結合方法",
    "step1Title": "ファイルをアップロード",
    "step1Text": "PDF ファイルをドロップゾーンにドラッグ＆ドロップするか、クリックして選択してください。",
    "step2Title": "並べ替えと設定",
    "step2Text": "ドラッグでファイルの順序を変更します。結合から除外するファイルはオフにします。",
    "step3Title": "結合してダウンロード",
    "step3Text": "結合ボタンをクリックして、すべての有効なファイルを1つの PDF に結合し、ダウンロードします。",
    "faq1Q": "ファイルサイズの制限はありますか？",
    "faq1A": "ありません。PDF の結合はブラウザ内で実行されるため、サーバー側のファイルサイズ制限はありません。非常に大きなファイルは処理に時間がかかる場合があります。",
    "faq2Q": "PDF 内のページ順序を変更できますか？",
    "faq2A": "いいえ。このツールは、配置した順序で PDF ファイル全体を結合します。各 PDF は1つの単位として扱われ、すべてのページがまとまります。",
    "faq3Q": "ファイルはサーバーにアップロードされますか？",
    "faq3A": "いいえ。すべての処理は pdf-lib ライブラリを使用してブラウザ内でローカルに実行されます。ファイルがデバイスを離れることはありません。"
  }
}
```

- [ ] **Step 9: Update ja/categories.json visual section**

Update the `"visual"` object in `public/locales/ja/categories.json` to include PDF Merger mention and update tool count.

- [ ] **Step 10: Add pdf-merge to ko/tools.json**

```json
  "pdf-merge": {
    "title": "PDF 병합 도구 - 여러 PDF를 하나로 병합",
    "shortTitle": "PDF 병합",
    "description": "여러 PDF 파일을 하나의 문서로 병합합니다. 드래그 앤 드롭으로 순서 변경, 썸네일 미리보기, 즉시 다운로드. 모든 처리는 브라우저에서 실행됩니다.",
    "searchTerms": "pdfhapche pdfhc hapche pdf moonseo"
  },
```

- [ ] **Step 11: Create ko/pdf-merge.json**

```json
{
  "dropPdf": "PDF 파일을 여기에 드롭하거나 클릭하여 선택",
  "supportedFormats": "PDF 파일만 지원",
  "addMoreFiles": "파일 추가",
  "mergeButton": "병합 및 다운로드",
  "mergeButtonInfo": "{count}개 파일 / {pages}페이지",
  "excludedCount": "{count}개 제외됨",
  "merging": "병합 중...",
  "mergeProgress": "전체 {total}개 중 {current}번째 파일 처리 중...",
  "mergeSuccess": "병합 완료!",
  "mergedResult": "{name} — {pages}페이지, {size}",
  "download": "다운로드",
  "newMerge": "새 병합",
  "onlyPdfSupported": "PDF 파일만 지원됩니다",
  "encryptedPdf": "이 PDF는 암호화되어 병합할 수 없습니다",
  "corruptedPdf": "이 PDF 파일이 손상되어 읽을 수 없습니다",
  "mergeFailed": "병합에 실패했습니다. 문제가 있는 파일을 제거하고 다시 시도하세요.",
  "pages": "{count}페이지",
  "descriptions": {
    "title": "PDF 병합 도구 소개",
    "aeoDefinition": "PDF 병합 도구는 여러 PDF 파일을 하나의 문서로 병합하는 무료 온라인 도구입니다. 드래그 앤 드롭으로 순서 변경, 썸네일 미리보기, 병합 결과 다운로드. 모든 처리는 브라우저에서 로컬로 실행됩니다.",
    "whatIsTitle": "PDF 병합 도구란?",
    "whatIs": "브라우저에서 여러 PDF 파일을 하나의 문서로 병합합니다. PDF를 업로드하고, 드래그로 순서를 변경하고, 개별 파일을 켜거나 끄고, 병합 결과를 다운로드합니다. 데이터는 서버에 업로드되지 않습니다 — 모든 처리는 pdf-lib 라이브러리를 사용합니다.",
    "useCases": "PDF 병합 도구 활용 시기",
    "useCasesP1": "여러 보고서, 송장 또는 계약서를 하나의 파일로 결합하여 쉽게 공유하세요.",
    "useCasesP2": "스캔한 문서나 인쇄물을 하나의 PDF로 병합하세요.",
    "stepsTitle": "PDF 병합 방법",
    "step1Title": "파일 업로드",
    "step1Text": "PDF 파일을 드롭 영역에 드래그 앤 드롭하거나 클릭하여 선택하세요.",
    "step2Title": "정렬 및 설정",
    "step2Text": "드래그하여 파일 순서를 변경합니다. 병합에서 제외할 파일은 끄세요.",
    "step3Title": "병합 및 다운로드",
    "step3Text": "병합 버튼을 클릭하여 모든 활성 파일을 하나의 PDF로 병합하고 다운로드합니다.",
    "faq1Q": "파일 크기 제한이 있나요?",
    "faq1A": "없습니다. PDF 병합은 브라우저에서 실행되므로 서버 측 파일 크기 제한이 없습니다. 매우 큰 파일은 처리에 시간이 더 걸릴 수 있습니다.",
    "faq2Q": "PDF 내의 페이지 순서를 변경할 수 있나요?",
    "faq2A": "아니요. 이 도구는 배치한 순서대로 전체 PDF 파일을 병합합니다. 각 PDF는 하나의 단위로 취급되며 모든 페이지가 함께 유지됩니다.",
    "faq3Q": "파일이 서버에 업로드되나요?",
    "faq3A": "아니요. 모든 처리는 pdf-lib 라이브러리를 사용하여 브라우저에서 로컬로 실행됩니다. 파일이 기기를 떠나지 않습니다."
  }
}
```

- [ ] **Step 12: Update ko/categories.json visual section**

Update the `"visual"` object in `public/locales/ko/categories.json` to include PDF Merger.

- [ ] **Step 13: Commit**

```bash
git add public/locales/zh-CN/ public/locales/zh-TW/ public/locales/ja/ public/locales/ko/
git commit -m "feat(pdf-merge): add CJK locale i18n (zh-CN, zh-TW, ja, ko)"
```

---

## Task 9: Create Latin-Script i18n Files (es, pt-BR, fr, de, ru)

**Files:**

- Create: `public/locales/es/pdf-merge.json`
- Create: `public/locales/pt-BR/pdf-merge.json`
- Create: `public/locales/fr/pdf-merge.json`
- Create: `public/locales/de/pdf-merge.json`
- Create: `public/locales/ru/pdf-merge.json`
- Modify: `public/locales/{es,pt-BR,fr,de,ru}/tools.json`
- Modify: `public/locales/{es,pt-BR,fr,de,ru}/categories.json`

Each locale needs:

1. `pdf-merge` entry in `tools.json` (no `searchTerms` — Latin-script shortTitle is searchable)
2. Full `pdf-merge.json` tool-specific file
3. Updated `visual` category in `categories.json`

- [ ] **Step 1: Add pdf-merge to es/tools.json**

```json
  "pdf-merge": {
    "title": "Fusionar PDF - Combinar Varios PDF en Uno",
    "shortTitle": "Fusionar PDF",
    "description": "Fusiona varios archivos PDF en un solo documento. Reordenamiento por arrastrar y soltar, vista previa de miniaturas y descarga instantánea. Todo el procesamiento se ejecuta en tu navegador."
  },
```

- [ ] **Step 2: Create es/pdf-merge.json**

```json
{
  "dropPdf": "Suelta archivos PDF aquí o haz clic para seleccionar",
  "supportedFormats": "Solo se admiten archivos PDF",
  "addMoreFiles": "Añadir más archivos",
  "mergeButton": "Fusionar y descargar",
  "mergeButtonInfo": "{count} archivos / {pages} páginas",
  "excludedCount": "{count} excluidos",
  "merging": "Fusionando...",
  "mergeProgress": "Procesando archivo {current} de {total}...",
  "mergeSuccess": "¡Fusionado con éxito!",
  "mergedResult": "{name} — {pages} páginas, {size}",
  "download": "Descargar",
  "newMerge": "Nueva fusión",
  "onlyPdfSupported": "Solo se admiten archivos PDF",
  "encryptedPdf": "Este PDF está cifrado y no se puede fusionar",
  "corruptedPdf": "Este archivo PDF está dañado y no se puede leer",
  "mergeFailed": "Error al fusionar. Intenta eliminar el archivo problemático.",
  "pages": "{count} páginas",
  "descriptions": {
    "title": "Sobre Fusionar PDF",
    "aeoDefinition": "Fusionar PDF es una herramienta online gratuita para combinar varios archivos PDF en un solo documento. Arrastra y suelta para reordenar, previsualiza miniaturas y descarga el resultado. Todo el procesamiento se ejecuta localmente en tu navegador.",
    "whatIsTitle": "¿Qué es Fusionar PDF?",
    "whatIs": "Combina varios archivos PDF en un solo documento directamente en tu navegador. Sube tus PDFs, arrastra para reordenar, activa o desactiva archivos individuales y descarga el resultado. No se suben datos a ningún servidor — todo el procesamiento usa la biblioteca pdf-lib.",
    "useCases": "Cuándo usar Fusionar PDF",
    "useCasesP1": "Combina varios informes, facturas o contratos en un solo archivo para compartir fácilmente.",
    "useCasesP2": "Fusiona documentos escaneados o impresiones en un PDF cohesionado.",
    "stepsTitle": "Cómo fusionar PDFs",
    "step1Title": "Subir archivos",
    "step1Text": "Arrastra y suelta tus archivos PDF o haz clic para seleccionarlos desde tu ordenador.",
    "step2Title": "Ordenar y configurar",
    "step2Text": "Arrastra para reordenar los archivos. Desactiva los archivos que quieras excluir de la fusión.",
    "step3Title": "Fusionar y descargar",
    "step3Text": "Haz clic en el botón Fusionar para combinar todos los archivos activados en un solo PDF y descargarlo.",
    "faq1Q": "¿Hay un límite de tamaño de archivo?",
    "faq1A": "No. La fusión de PDF se ejecuta completamente en tu navegador, por lo que no hay límite de tamaño de archivo del lado del servidor. Archivos muy grandes pueden tardar más en procesarse.",
    "faq2Q": "¿Puedo reordenar páginas dentro de un PDF?",
    "faq2A": "No. Esta herramienta fusiona archivos PDF completos en el orden que los organices. Cada PDF se trata como una unidad atómica — todas sus páginas se mantienen juntas.",
    "faq3Q": "¿Se suben mis archivos a un servidor?",
    "faq3A": "No. Todo el procesamiento ocurre localmente en tu navegador usando la biblioteca pdf-lib. Tus archivos nunca salen de tu dispositivo."
  }
}
```

- [ ] **Step 3: Update es/categories.json visual section**

Update tool count and add PDF Merger mention in the `visual` category.

- [ ] **Step 4: Add pdf-merge to pt-BR/tools.json**

```json
  "pdf-merge": {
    "title": "Mesclar PDF - Combinar Vários PDFs em Um",
    "shortTitle": "Mesclar PDF",
    "description": "Mescle vários arquivos PDF em um único documento. Reordenação por arrastar e soltar, pré-visualização de miniaturas e download instantâneo. Todo o processamento é feito no seu navegador."
  },
```

- [ ] **Step 5: Create pt-BR/pdf-merge.json**

```json
{
  "dropPdf": "Solte arquivos PDF aqui ou clique para selecionar",
  "supportedFormats": "Somente arquivos PDF são suportados",
  "addMoreFiles": "Adicionar mais arquivos",
  "mergeButton": "Mesclar e baixar",
  "mergeButtonInfo": "{count} arquivos / {pages} páginas",
  "excludedCount": "{count} excluídos",
  "merging": "Mesclando...",
  "mergeProgress": "Processando arquivo {current} de {total}...",
  "mergeSuccess": "Mesclado com sucesso!",
  "mergedResult": "{name} — {pages} páginas, {size}",
  "download": "Baixar",
  "newMerge": "Nova mesclagem",
  "onlyPdfSupported": "Somente arquivos PDF são suportados",
  "encryptedPdf": "Este PDF está criptografado e não pode ser mesclado",
  "corruptedPdf": "Este arquivo PDF está corrompido e não pode ser lido",
  "mergeFailed": "Falha na mesclagem. Tente remover o arquivo problemático.",
  "pages": "{count} páginas",
  "descriptions": {
    "title": "Sobre Mesclar PDF",
    "aeoDefinition": "Mesclar PDF é uma ferramenta online gratuita para combinar vários arquivos PDF em um único documento. Arraste e solte para reordenar, visualize miniaturas e baixe o resultado. Todo o processamento é feito localmente no seu navegador.",
    "whatIsTitle": "O que é o Mesclar PDF?",
    "whatIs": "Combine vários arquivos PDF em um único documento diretamente no seu navegador. Faça upload dos seus PDFs, arraste para reordenar, ative ou desative arquivos individuais e baixe o resultado. Nenhum dado é enviado a qualquer servidor — todo o processamento usa a biblioteca pdf-lib.",
    "useCases": "Quando usar o Mesclar PDF",
    "useCasesP1": "Combine vários relatórios, faturas ou contratos em um único arquivo para facilitar o compartilhamento.",
    "useCasesP2": "Mescle documentos digitalizados em um PDF coeso.",
    "stepsTitle": "Como mesclar PDFs",
    "step1Title": "Enviar arquivos",
    "step1Text": "Arraste e solte seus arquivos PDF ou clique para selecioná-los do seu computador.",
    "step2Title": "Organizar e configurar",
    "step2Text": "Arraste para reordenar os arquivos. Desative os arquivos que deseja excluir da mesclagem.",
    "step3Title": "Mesclar e baixar",
    "step3Text": "Clique no botão Mesclar para combinar todos os arquivos ativados em um único PDF e baixá-lo.",
    "faq1Q": "Existe limite de tamanho de arquivo?",
    "faq1A": "Não. A mesclagem de PDF é executada inteiramente no seu navegador, portanto não há limite de tamanho no servidor. Arquivos muito grandes podem levar mais tempo para processar.",
    "faq2Q": "Posso reordenar páginas dentro de um PDF?",
    "faq2A": "Não. Esta ferramenta mescla arquivos PDF inteiros na ordem que você organizar. Cada PDF é tratado como uma unidade atômica — todas as suas páginas ficam juntas.",
    "faq3Q": "Meus arquivos são enviados a um servidor?",
    "faq3A": "Não. Todo o processamento acontece localmente no seu navegador usando a biblioteca pdf-lib. Seus arquivos nunca saem do seu dispositivo."
  }
}
```

- [ ] **Step 6: Add pdf-merge to fr/tools.json**

```json
  "pdf-merge": {
    "title": "Fusionner PDF - Combiner Plusieurs PDF en Un Seul",
    "shortTitle": "Fusionner PDF",
    "description": "Fusionnez plusieurs fichiers PDF en un seul document. Réorganisation par glisser-déposer, aperçu des miniatures et téléchargement instantané. Tout le traitement s'exécute dans votre navigateur."
  },
```

- [ ] **Step 7: Create fr/pdf-merge.json**

```json
{
  "dropPdf": "Déposez vos fichiers PDF ici ou cliquez pour sélectionner",
  "supportedFormats": "Uniquement les fichiers PDF",
  "addMoreFiles": "Ajouter d'autres fichiers",
  "mergeButton": "Fusionner et télécharger",
  "mergeButtonInfo": "{count} fichiers / {pages} pages",
  "excludedCount": "{count} exclu(s)",
  "merging": "Fusion en cours...",
  "mergeProgress": "Traitement du fichier {current} sur {total}...",
  "mergeSuccess": "Fusion réussie !",
  "mergedResult": "{name} — {pages} pages, {size}",
  "download": "Télécharger",
  "newMerge": "Nouvelle fusion",
  "onlyPdfSupported": "Seuls les fichiers PDF sont pris en charge",
  "encryptedPdf": "Ce PDF est chiffré et ne peut pas être fusionné",
  "corruptedPdf": "Ce fichier PDF est corrompu et ne peut pas être lu",
  "mergeFailed": "Échec de la fusion. Essayez de supprimer le fichier problématique.",
  "pages": "{count} pages",
  "descriptions": {
    "title": "À propos de Fusionner PDF",
    "aeoDefinition": "Fusionner PDF est un outil en ligne gratuit pour combiner plusieurs fichiers PDF en un seul document. Glisser-déposer pour réorganiser, aperçu des miniatures et téléchargement du résultat. Tout le traitement s'exécute localement dans votre navigateur.",
    "whatIsTitle": "Qu'est-ce que Fusionner PDF ?",
    "whatIs": "Combinez plusieurs fichiers PDF en un seul document directement dans votre navigateur. Téléversez vos PDFs, glissez pour réorganiser, activez ou désactivez des fichiers individuels et téléchargez le résultat. Aucune donnée n'est envoyée à un serveur — tout le traitement utilise la bibliothèque pdf-lib.",
    "useCases": "Quand utiliser Fusionner PDF",
    "useCasesP1": "Combinez plusieurs rapports, factures ou contrats en un seul fichier pour un partage facile.",
    "useCasesP2": "Fusionnez des documents numérisés en un PDF cohérent.",
    "stepsTitle": "Comment fusionner des PDFs",
    "step1Title": "Téléverser les fichiers",
    "step1Text": "Glissez-déposez vos fichiers PDF ou cliquez pour les sélectionner depuis votre ordinateur.",
    "step2Title": "Organiser et configurer",
    "step2Text": "Glissez pour réorganiser les fichiers. Désactivez les fichiers que vous souhaitez exclure de la fusion.",
    "step3Title": "Fusionner et télécharger",
    "step3Text": "Cliquez sur le bouton Fusionner pour combiner tous les fichiers activés en un seul PDF et le télécharger.",
    "faq1Q": "Y a-t-il une limite de taille de fichier ?",
    "faq1A": "Non. La fusion de PDF s'exécute entièrement dans votre navigateur, il n'y a donc pas de limite côté serveur. Les fichiers très volumineux peuvent prendre plus de temps à traiter.",
    "faq2Q": "Puis-je réorganiser les pages dans un PDF ?",
    "faq2A": "Non. Cet outil fusionne des fichiers PDF entiers dans l'ordre que vous définissez. Chaque PDF est traité comme une unité atomique — toutes ses pages restent ensemble.",
    "faq3Q": "Mes fichiers sont-ils envoyés à un serveur ?",
    "faq3A": "Non. Tout le traitement se fait localement dans votre navigateur avec la bibliothèque pdf-lib. Vos fichiers ne quittent jamais votre appareil."
  }
}
```

- [ ] **Step 8: Add pdf-merge to de/tools.json**

```json
  "pdf-merge": {
    "title": "PDF Zusammenführen - Mehrere PDFs zu einem kombinieren",
    "shortTitle": "PDF Zusammenführen",
    "description": "Führen Sie mehrere PDF-Dateien zu einem Dokument zusammen. Drag-and-Drop-Sortierung, Vorschaubilder und sofortiger Download. Die gesamte Verarbeitung läuft in Ihrem Browser."
  },
```

- [ ] **Step 9: Create de/pdf-merge.json**

```json
{
  "dropPdf": "PDF-Dateien hier ablegen oder klicken zum Auswählen",
  "supportedFormats": "Nur PDF-Dateien werden unterstützt",
  "addMoreFiles": "Weitere Dateien hinzufügen",
  "mergeButton": "Zusammenführen & Herunterladen",
  "mergeButtonInfo": "{count} Dateien / {pages} Seiten",
  "excludedCount": "{count} ausgeschlossen",
  "merging": "Zusammenführen...",
  "mergeProgress": "Verarbeite Datei {current} von {total}...",
  "mergeSuccess": "Erfolgreich zusammengeführt!",
  "mergedResult": "{name} — {pages} Seiten, {size}",
  "download": "Herunterladen",
  "newMerge": "Neue Zusammenführung",
  "onlyPdfSupported": "Es werden nur PDF-Dateien unterstützt",
  "encryptedPdf": "Dieses PDF ist verschlüsselt und kann nicht zusammengeführt werden",
  "corruptedPdf": "Diese PDF-Datei ist beschädigt und kann nicht gelesen werden",
  "mergeFailed": "Zusammenführung fehlgeschlagen. Versuchen Sie, die problematische Datei zu entfernen.",
  "pages": "{count} Seiten",
  "descriptions": {
    "title": "Über PDF Zusammenführen",
    "aeoDefinition": "PDF Zusammenführen ist ein kostenloses Online-Tool zum Kombinieren mehrerer PDF-Dateien zu einem Dokument. Drag-and-Drop zum Sortieren, Vorschaubilder und Download des Ergebnisses. Die gesamte Verarbeitung läuft lokal in Ihrem Browser.",
    "whatIsTitle": "Was ist PDF Zusammenführen?",
    "whatIs": "Kombinieren Sie mehrere PDF-Dateien zu einem Dokument direkt in Ihrem Browser. Laden Sie Ihre PDFs hoch, sortieren Sie per Drag-and-Drop, schalten Sie einzelne Dateien ein oder aus und laden Sie das Ergebnis herunter. Es werden keine Daten an einen Server gesendet — die gesamte Verarbeitung verwendet die pdf-lib-Bibliothek.",
    "useCases": "Wann man PDF Zusammenführen verwendet",
    "useCasesP1": "Kombinieren Sie mehrere Berichte, Rechnungen oder Verträge in einer Datei für einfache Weitergabe.",
    "useCasesP2": "Führen Sie gescannte Dokumente zu einem zusammenhängenden PDF zusammen.",
    "stepsTitle": "Wie man PDFs zusammenführt",
    "step1Title": "Dateien hochladen",
    "step1Text": "Ziehen Sie Ihre PDF-Dateien per Drag-and-Drop oder klicken Sie, um sie von Ihrem Computer auszuwählen.",
    "step2Title": "Anordnen und konfigurieren",
    "step2Text": "Sortieren Sie die Dateien per Drag-and-Drop. Deaktivieren Sie Dateien, die von der Zusammenführung ausgeschlossen werden sollen.",
    "step3Title": "Zusammenführen und herunterladen",
    "step3Text": "Klicken Sie auf Zusammenführen, um alle aktivierten Dateien zu einem PDF zu kombinieren und es herunterzuladen.",
    "faq1Q": "Gibt es eine Dateigrößenbeschränkung?",
    "faq1A": "Nein. Die PDF-Zusammenführung läuft vollständig in Ihrem Browser ab, daher gibt es keine serverseitige Dateigrößenbeschränkung. Sehr große Dateien können länger dauern.",
    "faq2Q": "Kann ich Seiten innerhalb eines PDFs neu anordnen?",
    "faq2A": "Nein. Dieses Tool führt gesamte PDF-Dateien in der von Ihnen festgelegten Reihenfolge zusammen. Jedes PDF wird als atomare Einheit behandelt — alle Seiten bleiben zusammen.",
    "faq3Q": "Werden meine Dateien auf einen Server hochgeladen?",
    "faq3A": "Nein. Die gesamte Verarbeitung erfolgt lokal in Ihrem Browser mit der pdf-lib-Bibliothek. Ihre Dateien verlassen niemals Ihr Gerät."
  }
}
```

- [ ] **Step 10: Add pdf-merge to ru/tools.json**

```json
  "pdf-merge": {
    "title": "Объединение PDF - Соединить несколько PDF в один",
    "shortTitle": "Объединение PDF",
    "description": "Объедините несколько PDF-файлов в один документ. Перетаскивание для сортировки, предпросмотр миниатюр и мгновенная загрузка. Вся обработка выполняется в вашем браузере."
  },
```

- [ ] **Step 11: Create ru/pdf-merge.json**

```json
{
  "dropPdf": "Перетащите PDF-файлы сюда или нажмите для выбора",
  "supportedFormats": "Поддерживаются только PDF-файлы",
  "addMoreFiles": "Добавить ещё файлы",
  "mergeButton": "Объединить и скачать",
  "mergeButtonInfo": "{count} файлов / {pages} страниц",
  "excludedCount": "{count} исключено",
  "merging": "Объединение...",
  "mergeProgress": "Обработка файла {current} из {total}...",
  "mergeSuccess": "Успешно объединено!",
  "mergedResult": "{name} — {pages} страниц, {size}",
  "download": "Скачать",
  "newMerge": "Новое объединение",
  "onlyPdfSupported": "Поддерживаются только PDF-файлы",
  "encryptedPdf": "Этот PDF зашифрован и не может быть объединён",
  "corruptedPdf": "Этот PDF-файл повреждён и не может быть прочитан",
  "mergeFailed": "Ошибка объединения. Попробуйте удалить проблемный файл.",
  "pages": "{count} страниц",
  "descriptions": {
    "title": "Об объединении PDF",
    "aeoDefinition": "Объединение PDF — бесплатный онлайн-инструмент для объединения нескольких PDF-файлов в один документ. Перетаскивание для сортировки, предпросмотр миниатюр и загрузка результата. Вся обработка выполняется локально в вашем браузере.",
    "whatIsTitle": "Что такое объединение PDF?",
    "whatIs": "Объедините несколько PDF-файлов в один документ прямо в браузере. Загрузите PDF-файлы, перетащите для изменения порядка, включите или отключите отдельные файлы и скачайте результат. Данные не отправляются на сервер — вся обработка выполняется с помощью библиотеки pdf-lib.",
    "useCases": "Когда использовать объединение PDF",
    "useCasesP1": "Объедините несколько отчётов, счетов или договоров в один файл для удобного обмена.",
    "useCasesP2": "Объедините отсканированные документы в один PDF.",
    "stepsTitle": "Как объединить PDF",
    "step1Title": "Загрузите файлы",
    "step1Text": "Перетащите PDF-файлы в зону загрузки или нажмите, чтобы выбрать их на компьютере.",
    "step2Title": "Упорядочите и настройте",
    "step2Text": "Перетащите файлы для изменения порядка. Отключите файлы, которые нужно исключить из объединения.",
    "step3Title": "Объедините и скачайте",
    "step3Text": "Нажмите кнопку «Объединить», чтобы объединить все включённые файлы в один PDF и скачать его.",
    "faq1Q": "Есть ли ограничение на размер файла?",
    "faq1A": "Нет. Объединение PDF полностью выполняется в браузере, поэтому нет серверного ограничения на размер файла. Очень большие файлы могут обрабатываться дольше.",
    "faq2Q": "Можно ли изменить порядок страниц внутри PDF?",
    "faq2A": "Нет. Этот инструмент объединяет целые PDF-файлы в порядке, который вы задали. Каждый PDF обрабатывается как единое целое — все его страницы остаются вместе.",
    "faq3Q": "Загружаются ли мои файлы на сервер?",
    "faq3A": "Нет. Вся обработка выполняется локально в вашем браузере с помощью библиотеки pdf-lib. Ваши файлы никогда не покидают ваше устройство."
  }
}
```

- [ ] **Step 12: Update remaining categories.json files**

Update `visual` category in `public/locales/{es,pt-BR,fr,de,ru}/categories.json` — update tool count to 5 and add PDF Merger mention.

- [ ] **Step 13: Commit**

```bash
git add public/locales/es/ public/locales/pt-BR/ public/locales/fr/ public/locales/de/ public/locales/ru/
git commit -m "feat(pdf-merge): add Latin-script locale i18n (es, pt-BR, fr, de, ru)"
```

---

## Task 10: Create Route Entry (page.tsx)

**Files:**

- Create: `app/[locale]/pdf-merge/page.tsx`

- [ ] **Step 1: Create the route entry**

Create `app/[locale]/pdf-merge/page.tsx`:

```typescript
import { getTranslations } from "next-intl/server";
import { generatePageMeta } from "../../../libs/seo";
import { buildToolSchemas } from "../../../components/json-ld";
import { TOOLS, TOOL_CATEGORIES, CATEGORY_SLUGS } from "../../../libs/tools";
import PdfMergePage from "./pdf-merge-page";

const PATH = "/pdf-merge";
const TOOL_KEY = "pdf-merge";
const tool = TOOLS.find((t) => t.key === TOOL_KEY)!;

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  return generatePageMeta({
    locale,
    path: PATH,
    title: t("pdf-merge.title"),
    description: t("pdf-merge.description"),
    ogImage: { type: "tool", key: TOOL_KEY },
  });
}

export default async function PdfMergeRoute({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  const tx = await getTranslations({ locale, namespace: "pdf-merge" });
  const tc = await getTranslations({ locale, namespace: "categories" });
  const category = TOOL_CATEGORIES.find((c) => c.tools.includes(TOOL_KEY))!;
  const categorySlug = CATEGORY_SLUGS[category.key];

  const howToSteps = Array.from({ length: 3 }, (_, i) => ({
    name: tx(`descriptions.step${i + 1}Title`),
    text: tx(`descriptions.step${i + 1}Text`),
  })).filter((step) => step.name);

  const schemas = buildToolSchemas({
    name: t("pdf-merge.title"),
    description: tx.has("descriptions.aeoDefinition")
      ? tx("descriptions.aeoDefinition")
      : t("pdf-merge.description"),
    path: PATH,
    categoryName: tc(`${category.key}.shortTitle`),
    categoryPath: `/${categorySlug}`,
    faqItems: [1, 2, 3].map((i) => ({
      q: tx(`descriptions.faq${i}Q`),
      a: tx(`descriptions.faq${i}A`),
    })),
    howToSteps,
    sameAs: tool.sameAs,
  });

  return (
    <>
      {schemas.map((s, i) => (
        <script
          key={i}
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(s) }}
        />
      ))}
      <PdfMergePage />
    </>
  );
}
```

This follows the exact same pattern as `checksum/page.tsx` and `image-compress/page.tsx`.

- [ ] **Step 2: Commit**

```bash
git add app/\[locale\]/pdf-merge/page.tsx
git commit -m "feat(pdf-merge): add route entry with SEO metadata and JSON-LD"
```

---

## Task 11: Create Client Component (pdf-merge-page.tsx)

**Files:**

- Create: `app/[locale]/pdf-merge/pdf-merge-page.tsx`

This is the largest file. It contains:

- `Conversion` — main component with file list, drop zone, merge logic
- `Description` — thin wrapper around `DescriptionSection`
- `PdfMergePage` — default export wrapping in `Layout`

- [ ] **Step 1: Create the client component**

Create `app/[locale]/pdf-merge/pdf-merge-page.tsx`:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import { useTranslations } from "next-intl";
import Layout from "../../../components/layout";
import { showToast } from "../../../libs/toast";
import { fromEvent } from "file-selector";
import { formatBytes } from "../../../utils/storage";
import { Button } from "../../../components/ui/button";
import { Plus, X, GripVertical, Eye, EyeOff, Download, RotateCw } from "lucide-react";
import RelatedTools from "../../../components/related-tools";
import PrivacyBanner from "../../../components/privacy-banner";
import DescriptionSection from "../../../components/description-section";
import { mergePdfs, getPdfPageCount } from "../../../libs/pdf-merge/merge";
import { renderThumbnail } from "../../../libs/pdf-merge/thumbnail";
import type { PdfFileEntry, MergeProgress } from "../../../libs/pdf-merge/types";

const THUMBNAIL_CONCURRENCY = 3;

async function processFiles(
  files: File[],
  existingCount: number,
  onEntry: (entry: PdfFileEntry, index: number) => void
): Promise<void> {
  const entries: PdfFileEntry[] = [];

  // Read all files into ArrayBuffers first
  for (const file of files) {
    const arrayBuffer = await file.arrayBuffer();
    entries.push({
      id: crypto.randomUUID(),
      file,
      name: file.name,
      size: file.size,
      pageCount: 0,
      thumbnailUrl: null,
      arrayBuffer,
      enabled: true,
    });
  }

  // Process page counts and thumbnails with concurrency limit
  let running = 0;
  let idx = 0;

  await new Promise<void>((resolve) => {
    function next() {
      while (running < THUMBNAIL_CONCURRENCY && idx < entries.length) {
        const entry = entries[idx];
        const currentIdx = existingCount + idx;
        idx++;
        running++;

        (async () => {
          try {
            entry.pageCount = await getPdfPageCount(entry.arrayBuffer);
          } catch {
            entry.pageCount = 0;
          }
          onEntry(entry, currentIdx);

          try {
            entry.thumbnailUrl = await renderThumbnail(entry.arrayBuffer);
            onEntry(entry, currentIdx);
          } catch {
            // thumbnail rendering failed — entry already has null thumbnailUrl
          }

          running--;
          if (running === 0 && idx >= entries.length) resolve();
          else next();
        })();
      }
      if (running === 0 && idx >= entries.length) resolve();
    }
    next();
  });
}

function Conversion() {
  const t = useTranslations("pdf-merge");
  const tc = useTranslations("common");

  const [files, setFiles] = useState<PdfFileEntry[]>([]);
  const [merging, setMerging] = useState(false);
  const [progress, setProgress] = useState<MergeProgress | null>(null);
  const [resultBlob, setResultBlob] = useState<Blob | null>(null);
  const [error, setError] = useState<string | null>(null);

  const dropZoneRef = useRef<HTMLDivElement>(null);
  const fileInputRef = useRef<HTMLInputElement>(null);
  const addMoreInputRef = useRef<HTMLInputElement>(null);

  const dragItemRef = useRef<number | null>(null);
  const dragOverItemRef = useRef<number | null>(null);

  const enabledFiles = files.filter((f) => f.enabled);
  const totalPages = enabledFiles.reduce((sum, f) => sum + f.pageCount, 0);
  const canMerge = enabledFiles.length >= 2 && !merging;

  // Cleanup object URLs on unmount
  useEffect(() => {
    return () => {
      for (const f of files) {
        // thumbnailUrl is a data URL, no revocation needed
      }
    };
  }, []);

  // Drag-and-drop file handling
  useEffect(() => {
    const dropZone = dropZoneRef.current;
    if (!dropZone) return;

    const onDragOver = (e: DragEvent) => {
      e.preventDefault();
      e.stopPropagation();
    };
    const onDrop = async (e: DragEvent) => {
      e.preventDefault();
      e.stopPropagation();
      const dropped = await fromEvent(e);
      if (!dropped || dropped.length === 0) return;
      const pdfFiles = (dropped as File[]).filter((f) => f.name.toLowerCase().endsWith(".pdf"));
      if (pdfFiles.length === 0) {
        showToast(t("onlyPdfSupported"), "warning");
        return;
      }
      if (pdfFiles.length < (dropped as File[]).length) {
        showToast(t("onlyPdfSupported"), "warning");
      }
      await processFiles(pdfFiles, files.length, (entry, _idx) => {
        setFiles((prev) => {
          const existing = prev.findIndex((f) => f.id === entry.id);
          if (existing >= 0) {
            const updated = [...prev];
            updated[existing] = entry;
            return updated;
          }
          return [...prev, entry];
        });
      });
    };

    dropZone.addEventListener("dragover", onDragOver);
    dropZone.addEventListener("drop", onDrop);
    return () => {
      dropZone.removeEventListener("dragover", onDragOver);
      dropZone.removeEventListener("drop", onDrop);
    };
  }, [files.length, t]);

  async function handleFileInput(fileList: FileList | null, isAddMore = false) {
    if (!fileList || fileList.length === 0) return;
    const pdfFiles = Array.from(fileList).filter((f) => f.name.toLowerCase().endsWith(".pdf"));
    if (pdfFiles.length === 0) {
      showToast(t("onlyPdfSupported"), "warning");
      return;
    }
    const startIdx = isAddMore ? files.length : 0;
    await processFiles(pdfFiles, startIdx, (entry, _idx) => {
      setFiles((prev) => {
        const existing = prev.findIndex((f) => f.id === entry.id);
        if (existing >= 0) {
          const updated = [...prev];
          updated[existing] = entry;
          return updated;
        }
        return [...prev, entry];
      });
    });
  }

  function handleDelete(id: string) {
    setFiles((prev) => prev.filter((f) => f.id !== id));
  }

  function handleToggle(id: string) {
    setFiles((prev) => prev.map((f) => (f.id === id ? { ...f, enabled: !f.enabled } : f)));
  }

  function handleDragStart(index: number) {
    dragItemRef.current = index;
  }

  function handleDragEnter(index: number) {
    dragOverItemRef.current = index;
  }

  function handleDragEnd() {
    if (dragItemRef.current === null || dragOverItemRef.current === null) return;
    const from = dragItemRef.current;
    const to = dragOverItemRef.current;
    if (from === to) return;

    setFiles((prev) => {
      const updated = [...prev];
      const [moved] = updated.splice(from, 1);
      updated.splice(to, 0, moved);
      return updated;
    });

    dragItemRef.current = null;
    dragOverItemRef.current = null;
  }

  async function handleMerge() {
    if (!canMerge) return;
    setMerging(true);
    setProgress(null);
    setError(null);
    setResultBlob(null);

    try {
      const buffers = enabledFiles.map((f) => f.arrayBuffer);
      const result = await mergePdfs(buffers, (current, total) => {
        setProgress({ current, total });
      });
      const blob = new Blob([result], { type: "application/pdf" });
      setResultBlob(blob);
    } catch (e) {
      setError(t("mergeFailed"));
      showToast(t("mergeFailed"), "danger");
    } finally {
      setMerging(false);
      setProgress(null);
    }
  }

  function handleDownload() {
    if (!resultBlob) return;
    const url = URL.createObjectURL(resultBlob);
    const a = document.createElement("a");
    a.href = url;
    a.download = "merged.pdf";
    a.click();
    URL.revokeObjectURL(url);
  }

  function handleNewMerge() {
    setFiles([]);
    setResultBlob(null);
    setError(null);
    setProgress(null);
    setMerging(false);
    if (fileInputRef.current) fileInputRef.current.value = "";
    if (addMoreInputRef.current) addMoreInputRef.current.value = "";
  }

  // Result state
  if (resultBlob) {
    const resultPages = enabledFiles.reduce((sum, f) => sum + f.pageCount, 0);
    return (
      <section className="mt-4">
        <div className="rounded-xl border border-accent-cyan/30 bg-accent-cyan-dim/10 p-6 text-center">
          <div className="text-2xl mb-2">✅</div>
          <p className="text-fg-primary font-semibold text-lg mb-2">{t("mergeSuccess")}</p>
          <p className="text-fg-secondary text-sm mb-6">
            {t("mergedResult", {
              name: "merged.pdf",
              pages: resultPages,
              size: formatBytes(resultBlob.size),
            })}
          </p>
          <div className="flex items-center justify-center gap-3">
            <Button variant="primary" size="md" onClick={handleDownload}>
              <Download size={16} className="me-1.5" />
              {t("download")}
            </Button>
            <Button variant="outline" size="md" onClick={handleNewMerge}>
              <RotateCw size={16} className="me-1.5" />
              {t("newMerge")}
            </Button>
          </div>
        </div>
      </section>
    );
  }

  // Empty state — drop zone
  const dropZone = (
    <div
      ref={dropZoneRef}
      className="relative text-xl rounded-lg border-2 border-dashed border-accent-cyan/30 bg-accent-cyan-dim/10 text-accent-cyan"
      style={{ width: "100%", height: "12rem" }}
    >
      <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 flex flex-col items-center justify-center px-4 pointer-events-none">
        <span className="text-3xl mb-2">📑</span>
        <span className="font-bold">{t("dropPdf")}</span>
        <span className="text-sm mt-1 text-accent-cyan/70">{t("supportedFormats")}</span>
      </div>
      <input
        ref={fileInputRef}
        type="file"
        accept=".pdf"
        multiple
        className="opacity-0 absolute inset-0 w-full h-full cursor-pointer"
        onClick={() => {
          if (fileInputRef.current) fileInputRef.current.value = "";
        }}
        onChange={(e) => handleFileInput(e.target.files)}
      />
    </div>
  );

  return (
    <section className="mt-4">
      {files.length === 0 ? (
        dropZone
      ) : (
        <>
          {/* File list */}
          <div className="space-y-2">
            {files.map((file, index) => (
              <div
                key={file.id}
                draggable
                onDragStart={() => handleDragStart(index)}
                onDragEnter={() => handleDragEnter(index)}
                onDragEnd={handleDragEnd}
                onDragOver={(e) => e.preventDefault()}
                className={`flex items-center gap-3 rounded-lg border border-border-default bg-bg-surface p-3 transition-opacity ${
                  !file.enabled ? "opacity-50" : ""
                }`}
              >
                {/* Drag handle */}
                <button
                  type="button"
                  className="cursor-grab text-fg-muted hover:text-fg-secondary shrink-0"
                  aria-label="Drag to reorder"
                >
                  <GripVertical size={18} />
                </button>

                {/* Thumbnail */}
                <div className="w-[60px] h-[80px] shrink-0 rounded border border-border-default bg-bg-input flex items-center justify-center overflow-hidden">
                  {file.thumbnailUrl ? (
                    <img
                      src={file.thumbnailUrl}
                      alt={file.name}
                      className="w-full h-full object-contain"
                    />
                  ) : (
                    <div className="w-5 h-5 border-2 border-accent-cyan border-t-transparent rounded-full animate-spin" />
                  )}
                </div>

                {/* File info */}
                <div className="flex-1 min-w-0">
                  <p className="text-sm font-medium text-fg-primary truncate">{file.name}</p>
                  <p className="text-xs text-fg-muted mt-0.5">
                    {file.pageCount > 0 ? t("pages", { count: file.pageCount }) : "..."} ·{" "}
                    {formatBytes(file.size, 1000, 2)}
                  </p>
                </div>

                {/* Toggle */}
                <button
                  type="button"
                  onClick={() => handleToggle(file.id)}
                  className="shrink-0 text-fg-muted hover:text-fg-secondary transition-colors"
                  title={file.enabled ? "Exclude from merge" : "Include in merge"}
                >
                  {file.enabled ? <Eye size={18} /> : <EyeOff size={18} />}
                </button>

                {/* Delete */}
                <button
                  type="button"
                  onClick={() => handleDelete(file.id)}
                  className="shrink-0 text-fg-muted hover:text-danger transition-colors"
                  title="Remove"
                >
                  <X size={18} />
                </button>
              </div>
            ))}
          </div>

          {/* Add more button */}
          <div className="mt-3">
            <input
              ref={addMoreInputRef}
              type="file"
              accept=".pdf"
              multiple
              className="hidden"
              onChange={(e) => handleFileInput(e.target.files, true)}
            />
            <Button
              variant="outline"
              size="sm"
              onClick={() => {
                if (addMoreInputRef.current) addMoreInputRef.current.click();
              }}
            >
              <Plus size={14} className="me-1" />
              {t("addMoreFiles")}
            </Button>
          </div>

          {/* Merge progress bar */}
          {merging && progress && (
            <div className="mt-4">
              <div className="flex items-center justify-between text-xs text-fg-muted mb-1">
                <span>
                  {t("mergeProgress", { current: progress.current, total: progress.total })}
                </span>
                <span>{Math.round((progress.current / progress.total) * 100)}%</span>
              </div>
              <div className="w-full h-2 rounded-full bg-bg-input overflow-hidden">
                <div
                  className="h-full bg-accent-cyan rounded-full transition-all duration-300"
                  style={{ width: `${(progress.current / progress.total) * 100}%` }}
                />
              </div>
            </div>
          )}

          {/* Summary and merge button */}
          <div className="mt-4 flex flex-col sm:flex-row items-start sm:items-center gap-3">
            <div className="text-sm text-fg-secondary">
              <span className="font-medium">
                {t("mergeButtonInfo", { count: enabledFiles.length, pages: totalPages })}
              </span>
              {files.length - enabledFiles.length > 0 && (
                <span className="ml-2 text-fg-muted">
                  ({t("excludedCount", { count: files.length - enabledFiles.length })})
                </span>
              )}
            </div>
            <Button
              variant="primary"
              size="lg"
              disabled={!canMerge}
              onClick={handleMerge}
              className="rounded-full uppercase font-bold"
            >
              {merging ? t("merging") : t("mergeButton")}
            </Button>
          </div>

          {error && <p className="mt-2 text-sm text-danger">{error}</p>}
        </>
      )}
    </section>
  );
}

function Description() {
  return <DescriptionSection namespace="pdf-merge" />;
}

export default function PdfMergePage() {
  const t = useTranslations("tools");
  const title = t("pdf-merge.shortTitle");
  return (
    <Layout title={title} categoryLabel={t("categories.visual")} categorySlug="visual-media">
      <div className="container mx-auto px-4 pt-3 pb-6">
        <PrivacyBanner variant="files" />
        <Conversion />
        <Description />
        <RelatedTools currentTool="pdf-merge" />
      </div>
    </Layout>
  );
}
```

Notes on the implementation:

- **HTML5 Drag and Drop** for reorder (not a library) — uses `draggable`, `onDragStart`, `onDragEnter`, `onDragEnd`
- **`file-selector`'s `fromEvent`** for file drop handling (consistent with checksum tool)
- **Hidden file inputs** — one for initial drop zone, one for "Add more files" button
- **Thumbnail concurrency** limited to 3 via custom queue in `processFiles`
- **State machine**: empty → files loaded → merging (with progress) → result → back to empty via "New Merge"
- **Data URLs** for thumbnails (returned by `renderThumbnail`) — no revocation needed
- **Error handling**: PDF load errors caught, friendly messages via toast

- [ ] **Step 2: Run LSP diagnostics on the file**

Run:

```bash
npx tsc --noEmit app/\[locale\]/pdf-merge/pdf-merge-page.tsx
```

Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add app/\[locale\]/pdf-merge/pdf-merge-page.tsx
git commit -m "feat(pdf-merge): add client component with drag reorder and merge UI"
```

---

## Task 12: Final Verification

**Files:**

- All files from Tasks 1–11

- [ ] **Step 1: Run all tests**

Run:

```bash
npm test
```

Expected: All tests pass, including new `libs/pdf-merge/__tests__/merge.test.ts` and updated `libs/__tests__/tool-relations.test.ts`.

- [ ] **Step 2: Run the dev server**

Run:

```bash
npm run dev
```

Then open `http://localhost:3000/pdf-merge` and verify:

1. Drop zone appears with 📑 icon and text
2. Uploading PDFs shows file list with thumbnails
3. Drag reordering works
4. Eye toggle disables/enables files
5. Delete button removes files
6. "Add more files" button works
7. Merge button shows file/page count, disabled when < 2 enabled files
8. Merging shows progress bar
9. Result shows with download and new merge buttons
10. Description section and related tools render

- [ ] **Step 3: Run build**

Run:

```bash
npm run build
```

Expected: Build succeeds with no errors.

- [ ] **Step 4: Final commit (if any fixes needed)**

```bash
git add -A
git commit -m "fix(pdf-merge): address build and runtime issues"
```
