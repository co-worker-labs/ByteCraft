# SEO/AEO P2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add AEO answer-first definitions, category aggregation pages, and semantic inline links to all 32 OmniKit tools (en + zh-CN).

**Architecture:** Progressive enhancement on existing architecture. No new abstraction layers. `libs/tools.ts` gets new constants (`CATEGORY_SLUGS`, `TOOL_PATHS`, `getToolCategory`). `components/json-ld.tsx` gets `buildCategorySchema()` and 3-level breadcrumb support. New `utils/linked-text.tsx` renders markdown-style `[text](/path)` links. New `components/category-page.tsx` shared component rendered by 6 category route `page.tsx` files. Each tool's `page.tsx` passes category info to `buildToolSchemas()`. Each tool's `*-page.tsx` Description gets AEO paragraph + category badge + `renderLinkedText()` on selected paragraphs.

**Tech Stack:** Next.js 16 App Router, TypeScript, next-intl, Tailwind CSS, Vitest

---

## File Structure

| File                                        | Responsibility                                        | Status |
| ------------------------------------------- | ----------------------------------------------------- | ------ |
| `libs/tools.ts`                             | Add `CATEGORY_SLUGS`, `TOOL_PATHS`, `getToolCategory` | Modify |
| `components/json-ld.tsx`                    | Add `buildCategorySchema`, 3-level breadcrumb         | Modify |
| `utils/linked-text.tsx`                     | `renderLinkedText()` utility                          | Create |
| `components/category-page.tsx`              | Shared category page component                        | Create |
| `i18n/request.ts`                           | Add `"categories"` namespace                          | Modify |
| `public/locales/en/categories.json`         | English category content                              | Create |
| `public/locales/zh-CN/categories.json`      | Chinese category content                              | Create |
| `app/[locale]/text-processing/page.tsx`     | Text category route                                   | Create |
| `app/[locale]/encoding-conversion/page.tsx` | Encoding category route                               | Create |
| `app/[locale]/security-crypto/page.tsx`     | Security category route                               | Create |
| `app/[locale]/generators/page.tsx`          | Generators category route                             | Create |
| `app/[locale]/visual-media/page.tsx`        | Visual category route                                 | Create |
| `app/[locale]/reference-lookup/page.tsx`    | Reference category route                              | Create |
| `app/sitemap.ts`                            | Add 6 category pages                                  | Modify |
| `app/[locale]/home-page.tsx`                | Category headers → links                              | Modify |
| 32 × `app/[locale]/{tool}/page.tsx`         | Pass category info + aeoDefinition                    | Modify |
| 32 × `app/[locale]/{tool}/{tool}-page.tsx`  | Add AEO paragraph + badge + renderLinkedText          | Modify |
| 32 × `public/locales/en/{tool}.json`        | Add aeoDefinition + inline links                      | Modify |
| 32 × `public/locales/zh-CN/{tool}.json`     | Add aeoDefinition + inline links                      | Modify |
| `libs/__tests__/tools-aeo.test.ts`          | Tests for new exports                                 | Create |
| `libs/__tests__/json-ld.test.ts`            | Tests for buildCategorySchema + 3-level breadcrumb    | Modify |
| `utils/__tests__/linked-text.test.tsx`      | Tests for renderLinkedText                            | Create |

---

## Per-Tool Reference Table

The following table is used throughout Phase 2 and Phase 3. It maps each tool to its category, PATH, and the tool-specific namespace variable name used in its `page.tsx`.

| Tool Key    | Category   | PATH         | Namespace Var |
| ----------- | ---------- | ------------ | ------------- |
| json        | text       | /json        | tx            |
| regex       | text       | /regex       | tx            |
| diff        | text       | /diff        | tx            |
| markdown    | text       | /markdown    | tx            |
| textcase    | text       | /textcase    | tx            |
| extractor   | text       | /extractor   | tx            |
| wordcounter | text       | /wordcounter | tx            |
| deduplines  | text       | /deduplines  | tx            |
| base64      | encoding   | /base64      | tb            |
| urlencoder  | encoding   | /urlencoder  | tx            |
| csv         | encoding   | /csv         | tx            |
| csv-md      | encoding   | /csv-md      | tx            |
| numbase     | encoding   | /numbase     | tx            |
| yaml        | encoding   | /yaml        | tx            |
| storageunit | encoding   | /storageunit | tx            |
| jwt         | security   | /jwt         | tx            |
| hashing     | security   | /hashing     | tx            |
| password    | security   | /password    | tp            |
| sshkey      | security   | /sshkey      | ts            |
| cipher      | security   | /cipher      | tx            |
| checksum    | security   | /checksum    | tx            |
| uuid        | generators | /uuid        | tx            |
| cron        | generators | /cron        | tx            |
| unixtime    | generators | /unixtime    | tx            |
| qrcode      | generators | /qrcode      | tx            |
| color       | visual     | /color       | tx            |
| image       | visual     | /image       | tx            |
| httpstatus  | reference  | /httpstatus  | tx            |
| httpclient  | reference  | /httpclient  | tx            |
| dbviewer    | reference  | /dbviewer    | tx            |
| ascii       | reference  | /ascii       | tx            |
| htmlcode    | reference  | /htmlcode    | tx            |

---

## Phase 1: Architecture Infrastructure

### Task 1: libs/tools.ts — CATEGORY_SLUGS, TOOL_PATHS, getToolCategory

**Files:**

- Modify: `libs/tools.ts:52-57` (after `ToolCategory` type)
- Test: `libs/__tests__/tools-aeo.test.ts`

- [ ] **Step 1: Write the failing tests**

Create `libs/__tests__/tools-aeo.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { CATEGORY_SLUGS, TOOL_PATHS, getToolCategory, TOOLS, TOOL_CATEGORIES } from "../tools";

describe("CATEGORY_SLUGS", () => {
  it("has a slug for every ToolCategory", () => {
    const categories: string[] = TOOL_CATEGORIES.map((c) => c.key);
    for (const cat of categories) {
      expect(
        CATEGORY_SLUGS[cat as keyof typeof CATEGORY_SLUGS],
        `Missing slug for ${cat}`
      ).toBeDefined();
    }
  });

  it("slugs are lowercase-with-hyphens", () => {
    for (const slug of Object.values(CATEGORY_SLUGS)) {
      expect(slug).toMatch(/^[a-z]+(-[a-z]+)*$/);
    }
  });
});

describe("TOOL_PATHS", () => {
  it("contains every tool path from TOOLS", () => {
    for (const tool of TOOLS) {
      expect(TOOL_PATHS.has(tool.path), `Missing path ${tool.path}`).toBe(true);
    }
  });

  it("size matches TOOLS length", () => {
    expect(TOOL_PATHS.size).toBe(TOOLS.length);
  });
});

describe("getToolCategory", () => {
  it("returns correct category for each tool", () => {
    for (const cat of TOOL_CATEGORIES) {
      for (const toolKey of cat.tools) {
        expect(getToolCategory(toolKey), `${toolKey} should be ${cat.key}`).toBe(cat.key);
      }
    }
  });

  it("returns undefined for unknown tool", () => {
    expect(getToolCategory("nonexistent")).toBeUndefined();
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run libs/__tests__/tools-aeo.test.ts`
Expected: FAIL — `CATEGORY_SLUGS`, `TOOL_PATHS`, `getToolCategory` are not exported

- [ ] **Step 3: Implement the exports**

Add after the `CategoryGroup` interface (after line 57) in `libs/tools.ts`:

```ts
export const CATEGORY_SLUGS: Record<ToolCategory, string> = {
  text: "text-processing",
  encoding: "encoding-conversion",
  security: "security-crypto",
  generators: "generators",
  visual: "visual-media",
  reference: "reference-lookup",
};

export const TOOL_PATHS = new Set(TOOLS.map((t) => t.path));

export function getToolCategory(toolKey: string): ToolCategory | undefined {
  return TOOL_CATEGORIES.find((c) => c.tools.includes(toolKey))?.key;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run libs/__tests__/tools-aeo.test.ts`
Expected: PASS (all 7 tests)

- [ ] **Step 5: Commit**

```bash
git add libs/tools.ts libs/__tests__/tools-aeo.test.ts
git commit -m "feat(seo): add CATEGORY_SLUGS, TOOL_PATHS, getToolCategory"
```

---

### Task 2: components/json-ld.tsx — buildCategorySchema + 3-level breadcrumb

**Files:**

- Modify: `components/json-ld.tsx:41-47` (buildToolSchemas signature and body)
- Modify: `libs/__tests__/json-ld.test.ts`

- [ ] **Step 1: Write the failing tests**

Add to `libs/__tests__/json-ld.test.ts` (append new describe blocks after existing ones):

```ts
import { buildCategorySchema } from "../../components/json-ld";

describe("buildToolSchemas — 3-level breadcrumb", () => {
  const base = {
    name: "JSON Formatter",
    description: "Format and validate JSON online",
    path: "/json",
    categoryName: "Text Processing",
    categoryPath: "/text-processing",
  };

  it("renders 3-level breadcrumb when categoryName + categoryPath provided", () => {
    const schemas = buildToolSchemas(base);
    const breadcrumb = schemas[1] as Record<string, unknown>;
    expect(breadcrumb["@type"]).toBe("BreadcrumbList");
    const items = breadcrumb.itemListElement as Record<string, unknown>[];
    expect(items).toHaveLength(3);
    expect(items[0]).toEqual({
      "@type": "ListItem",
      position: 1,
      name: "OmniKit",
      item: "https://omnikit.run",
    });
    expect(items[1]).toEqual({
      "@type": "ListItem",
      position: 2,
      name: "Text Processing",
      item: "https://omnikit.run/text-processing",
    });
    expect(items[2]).toEqual({
      "@type": "ListItem",
      position: 3,
      name: "JSON Formatter",
      item: "https://omnikit.run/json",
    });
  });

  it("falls back to 2-level when no category info", () => {
    const schemas = buildToolSchemas({
      name: "JSON Formatter",
      description: "Format and validate JSON online",
      path: "/json",
    });
    const breadcrumb = schemas[1] as Record<string, unknown>;
    const items = breadcrumb.itemListElement as Record<string, unknown>[];
    expect(items).toHaveLength(2);
  });
});

describe("buildCategorySchema", () => {
  const base = {
    name: "Text Processing",
    description: "Free online text processing tools.",
    path: "/text-processing",
    tools: [
      { name: "JSON Formatter", url: "https://omnikit.run/json" },
      { name: "Regex Tester", url: "https://omnikit.run/regex" },
    ],
  };

  it("returns CollectionPage + ItemList + BreadcrumbList schemas", () => {
    const schemas = buildCategorySchema(base);
    expect(schemas).toHaveLength(3);
    expect((schemas[0] as Record<string, unknown>)["@type"]).toBe("CollectionPage");
    expect((schemas[1] as Record<string, unknown>)["@type"]).toBe("ItemList");
    expect((schemas[2] as Record<string, unknown>)["@type"]).toBe("BreadcrumbList");
  });

  it("returns FAQPage when faqItems provided", () => {
    const schemas = buildCategorySchema({
      ...base,
      faqItems: [{ q: "What is this?", a: "A collection" }],
    });
    expect(schemas).toHaveLength(4);
    const faq = schemas[3] as Record<string, unknown>;
    expect(faq["@type"]).toBe("FAQPage");
  });

  it("BreadcrumbList is 2-level (OmniKit > Category)", () => {
    const schemas = buildCategorySchema(base);
    const breadcrumb = schemas[2] as Record<string, unknown>;
    const items = breadcrumb.itemListElement as Record<string, unknown>[];
    expect(items).toHaveLength(2);
    expect(items[0].name).toBe("OmniKit");
    expect(items[1].name).toBe("Text Processing");
  });

  it("ItemList has ordered tools", () => {
    const schemas = buildCategorySchema(base);
    const itemList = schemas[1] as Record<string, unknown>;
    const elements = itemList.itemListElement as Record<string, unknown>[];
    expect(elements).toHaveLength(2);
    expect(elements[0].position).toBe(1);
    expect(elements[1].position).toBe(2);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run libs/__tests__/json-ld.test.ts`
Expected: FAIL — `buildCategorySchema` not exported, `categoryName` not accepted

- [ ] **Step 3: Update buildToolSchemas signature for 3-level breadcrumb**

Replace the `buildToolSchemas` function in `components/json-ld.tsx` (lines 41-102) with:

```tsx
export function buildToolSchemas(options: {
  name: string;
  description: string;
  path: string;
  faqItems?: { q: string; a: string }[];
  howToSteps?: { name: string; text: string }[];
  categoryName?: string;
  categoryPath?: string;
}): object[] {
  const { name, description, path, faqItems, howToSteps, categoryName, categoryPath } = options;
  const url = `${SITE_URL}${path}`;

  const schemas: object[] = [];

  schemas.push({
    "@context": "https://schema.org",
    "@type": ["WebApplication", "SoftwareApplication"],
    name,
    description,
    url,
    applicationCategory: "DeveloperApplication",
    operatingSystem: "Any",
    offers: { "@type": "Offer", price: "0", priceCurrency: "USD" },
    browserRequirements: "Requires JavaScript. Requires HTML5.",
  });

  const breadcrumbItems: object[] = [
    { "@type": "ListItem", position: 1, name: "OmniKit", item: SITE_URL },
  ];
  if (categoryName && categoryPath) {
    breadcrumbItems.push({
      "@type": "ListItem",
      position: 2,
      name: categoryName,
      item: `${SITE_URL}${categoryPath}`,
    });
    breadcrumbItems.push({ "@type": "ListItem", position: 3, name, item: url });
  } else {
    breadcrumbItems.push({ "@type": "ListItem", position: 2, name, item: url });
  }

  schemas.push({
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: breadcrumbItems,
  });

  if (faqItems && faqItems.length > 0) {
    schemas.push({
      "@context": "https://schema.org",
      "@type": "FAQPage",
      mainEntity: faqItems.map((item) => ({
        "@type": "Question",
        name: item.q,
        acceptedAnswer: { "@type": "Answer", text: item.a },
      })),
    });
  }

  if (howToSteps && howToSteps.length > 0) {
    schemas.push({
      "@context": "https://schema.org",
      "@type": "HowTo",
      name,
      description,
      step: howToSteps.map((step, i) => ({
        "@type": "HowToStep",
        position: i + 1,
        name: step.name,
        text: step.text,
      })),
    });
  }

  return schemas;
}
```

- [ ] **Step 4: Add buildCategorySchema function**

Append to `components/json-ld.tsx` after `buildToolSchemas`:

```tsx
export function buildCategorySchema(options: {
  name: string;
  description: string;
  path: string;
  tools: { name: string; url: string }[];
  faqItems?: { q: string; a: string }[];
}): object[] {
  const { name, description, path, tools, faqItems } = options;
  const url = `${SITE_URL}${path}`;

  const schemas: object[] = [];

  schemas.push({
    "@context": "https://schema.org",
    "@type": "CollectionPage",
    name,
    description,
    url,
  });

  schemas.push({
    "@context": "https://schema.org",
    "@type": "ItemList",
    itemListElement: tools.map((tool, i) => ({
      "@type": "ListItem",
      position: i + 1,
      name: tool.name,
      url: tool.url,
    })),
  });

  schemas.push({
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    itemListElement: [
      { "@type": "ListItem", position: 1, name: "OmniKit", item: SITE_URL },
      { "@type": "ListItem", position: 2, name, item: url },
    ],
  });

  if (faqItems && faqItems.length > 0) {
    schemas.push({
      "@context": "https://schema.org",
      "@type": "FAQPage",
      mainEntity: faqItems.map((item) => ({
        "@type": "Question",
        name: item.q,
        acceptedAnswer: { "@type": "Answer", text: item.a },
      })),
    });
  }

  return schemas;
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run libs/__tests__/json-ld.test.ts`
Expected: PASS (all old + new tests)

- [ ] **Step 6: Commit**

```bash
git add components/json-ld.tsx libs/__tests__/json-ld.test.ts
git commit -m "feat(seo): add buildCategorySchema + 3-level breadcrumb"
```

---

### Task 3: utils/linked-text.tsx — renderLinkedText

**Files:**

- Create: `utils/linked-text.tsx`
- Create: `utils/__tests__/linked-text.test.tsx`

- [ ] **Step 1: Write the failing tests**

Create `utils/__tests__/linked-text.test.tsx`:

```tsx
import { describe, it, expect } from "vitest";
import { renderLinkedText } from "../linked-text";
import { TOOL_PATHS } from "../../libs/tools";
import React from "react";

describe("renderLinkedText", () => {
  it("returns plain text when no links", () => {
    const result = renderLinkedText("Hello world", "en");
    expect(result).toEqual(["Hello world"]);
  });

  it("renders a valid tool path as a link element", () => {
    const result = renderLinkedText("Use the [CSV converter](/csv) to convert.", "en");
    expect(result).toHaveLength(3);
    expect(result[0]).toBe("Use the ");
    expect(result[2]).toBe(" to convert.");
    const link = result[1] as React.ReactElement;
    expect(link.type).toBe("a");
    expect(link.props.href).toBe("/csv");
    expect(link.props.children).toBe("CSV converter");
  });

  it("adds locale prefix for non-en locale", () => {
    const result = renderLinkedText("Use the [CSV converter](/csv) now.", "zh-CN");
    const link = result[1] as React.ReactElement;
    expect(link.props.href).toBe("/zh-CN/csv");
  });

  it("leaves invalid paths as plain text", () => {
    const result = renderLinkedText("Use the [broken](/nonexistent) link.", "en");
    expect(result).toHaveLength(1);
    expect(result[0]).toBe("Use the [broken](/nonexistent) link.");
  });

  it("handles multiple links", () => {
    const result = renderLinkedText("Use [JSON](/json) and [CSV](/csv) tools.", "en");
    const links = result.filter((r) => React.isValidElement(r));
    expect(links).toHaveLength(2);
  });

  it("handles adjacent links with no text between", () => {
    const result = renderLinkedText("[JSON](/json)[CSV](/csv)", "en");
    const links = result.filter((r) => React.isValidElement(r));
    expect(links).toHaveLength(2);
    const emptyStrings = result.filter((r) => typeof r === "string" && r === "");
    expect(emptyStrings.length).toBeGreaterThanOrEqual(0);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run utils/__tests__/linked-text.test.tsx`
Expected: FAIL — module not found

- [ ] **Step 3: Implement renderLinkedText**

Create `utils/linked-text.tsx`:

```tsx
import React from "react";
import { TOOL_PATHS } from "../libs/tools";

export function renderLinkedText(text: string, locale: string): React.ReactNode[] {
  const prefix = locale === "en" ? "" : `/${locale}`;
  const pattern = /\[([^\]]+)\]\((\/[^)]+)\)/g;

  const result: React.ReactNode[] = [];
  let lastIndex = 0;
  let match: RegExpExecArray | null;
  let key = 0;

  while ((match = pattern.exec(text)) !== null) {
    const linkText = match[1];
    const path = match[2];

    if (match.index > lastIndex) {
      result.push(text.slice(lastIndex, match.index));
    }

    if (TOOL_PATHS.has(path)) {
      result.push(
        React.createElement(
          "a",
          {
            key: `link-${key++}`,
            href: `${prefix}${path}`,
            className: "text-accent-cyan hover:underline",
          },
          linkText
        )
      );
    } else {
      result.push(match[0]);
    }

    lastIndex = match.index + match[0].length;
  }

  if (lastIndex < text.length) {
    result.push(text.slice(lastIndex));
  }

  if (result.length === 0) {
    result.push(text);
  }

  return result;
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run utils/__tests__/linked-text.test.tsx`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add utils/linked-text.tsx utils/__tests__/linked-text.test.tsx
git commit -m "feat(seo): add renderLinkedText utility for inline tool links"
```

---

### Task 4: i18n/request.ts — Add categories namespace

**Files:**

- Modify: `i18n/request.ts:4-43`

- [ ] **Step 1: Add "categories" to namespaces array**

In `i18n/request.ts`, add `"categories"` after `"tools"` in the namespaces array:

```ts
const namespaces = [
  "common",
  "tools",
  "categories",
  "home",
  // ... rest unchanged
];
```

- [ ] **Step 2: Create placeholder locale files**

The namespace loader dynamically imports `public/locales/{locale}/categories.json`. Create minimal placeholder files so the import doesn't fail for any locale.

Create `public/locales/en/categories.json`:

```json
{}
```

Create `public/locales/zh-CN/categories.json`:

```json
{}
```

For the remaining 8 locales, create identical empty `{}` files:

```bash
for locale in zh-TW ja ko es pt-BR fr de ru; do
  echo '{}' > "public/locales/$locale/categories.json"
done
```

- [ ] **Step 3: Verify dev server starts**

Run: `npx next build 2>&1 | head -20`
Expected: Build succeeds without errors

- [ ] **Step 4: Commit**

```bash
git add i18n/request.ts public/locales/*/categories.json
git commit -m "feat(seo): register categories namespace with placeholder files"
```

---

### Task 5: components/category-page.tsx — Shared category page component

**Files:**

- Create: `components/category-page.tsx`

- [ ] **Step 1: Create the shared component**

Create `components/category-page.tsx`:

```tsx
"use client";

import Link from "next/link";
import Layout from "./layout";
import { useLocale, useTranslations } from "next-intl";
import {
  TOOL_CATEGORIES,
  CATEGORY_SLUGS,
  getToolCards,
  getToolCardMap,
  getToolCardsByKeys,
  getToolIconColor,
  type ToolCategory,
} from "../libs/tools";
import { Card } from "./ui/card";
import { Accordion } from "./ui/accordion";

export default function CategoryPage({ categoryKey }: { categoryKey: ToolCategory }) {
  const tc = useTranslations("categories");
  const t = useTranslations("tools");
  const locale = useLocale();
  const prefix = locale === "en" ? "" : `/${locale}`;

  const category = TOOL_CATEGORIES.find((c) => c.key === categoryKey)!;
  const cardMap = getToolCardMap(t);
  const tools = getToolCardsByKeys(category.tools, cardMap);

  const intro = tc.has(`${categoryKey}.intro`) ? tc(`${categoryKey}.intro`) : "";
  const faqItems = [1, 2, 3]
    .map((i) =>
      tc.has(`${categoryKey}.faq${i}Q`)
        ? { title: tc(`${categoryKey}.faq${i}Q`), content: <p>{tc(`${categoryKey}.faq${i}A`)}</p> }
        : null
    )
    .filter((item): item is NonNullable<typeof item> => item !== null);

  return (
    <Layout headerPosition="sticky">
      <section className="container mx-auto px-4 pt-6 pb-20">
        <div className="max-w-4xl mx-auto">
          <h1 className="text-2xl md:text-3xl font-mono font-bold text-fg-primary tracking-tight">
            {tc(`${categoryKey}.shortTitle`)}
          </h1>

          {intro && (
            <p className="mt-4 text-fg-secondary text-sm leading-relaxed max-w-3xl">{intro}</p>
          )}

          <div className="mt-8 grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">
            {tools.map((tool) => {
              const Icon = tool.icon;
              return (
                <Link key={tool.path} href={`${prefix}${tool.path}`}>
                  <Card hover className="group flex flex-col items-center cursor-pointer h-full">
                    <div className="flex flex-1 flex-col items-center p-2">
                      {Icon && (
                        <div
                          className="mb-4 flex h-14 w-14 items-center justify-center rounded-xl transition-colors group-hover:brightness-110"
                          style={{ backgroundColor: `${getToolIconColor(tool.path)}15` }}
                        >
                          <Icon size={28} style={{ color: getToolIconColor(tool.path) }} />
                        </div>
                      )}
                      <h3 className="font-semibold text-fg-primary text-center">{tool.title}</h3>
                      <p className="mt-2 line-clamp-2 text-sm text-fg-secondary text-center leading-relaxed">
                        {tool.description}
                      </p>
                    </div>
                  </Card>
                </Link>
              );
            })}
          </div>

          {faqItems.length > 0 && (
            <div className="mt-12">
              <h2 className="text-base font-semibold text-fg-primary mb-4">FAQ</h2>
              <Accordion items={faqItems} />
            </div>
          )}
        </div>
      </section>
    </Layout>
  );
}
```

- [ ] **Step 2: Verify build**

Run: `npx next build 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 3: Commit**

```bash
git add components/category-page.tsx
git commit -m "feat(seo): add shared CategoryPage component"
```

---

### Task 6: Six category route page.tsx files

**Files:**

- Create: `app/[locale]/text-processing/page.tsx`
- Create: `app/[locale]/encoding-conversion/page.tsx`
- Create: `app/[locale]/security-crypto/page.tsx`
- Create: `app/[locale]/generators/page.tsx`
- Create: `app/[locale]/visual-media/page.tsx`
- Create: `app/[locale]/reference-lookup/page.tsx`

- [ ] **Step 1: Create text-processing route**

Create `app/[locale]/text-processing/page.tsx`:

```tsx
import { getTranslations } from "next-intl/server";
import { generatePageMeta } from "../../../libs/seo";
import { buildCategorySchema } from "../../../components/json-ld";
import { TOOL_CATEGORIES, CATEGORY_SLUGS, TOOLS } from "../../../libs/tools";
import { SITE_URL } from "../../../libs/site";
import CategoryPage from "../../../components/category-page";

const CATEGORY_KEY = "text" as const;
const PATH = "/text-processing";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "categories" });
  return generatePageMeta({
    locale,
    path: PATH,
    title: t(`${CATEGORY_KEY}.title`),
    description: t(`${CATEGORY_KEY}.description`),
  });
}

export default async function TextProcessingRoute({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  const tc = await getTranslations({ locale, namespace: "categories" });

  const categoryTools = TOOL_CATEGORIES.find((c) => c.key === CATEGORY_KEY)!;
  const toolSchemas = categoryTools.tools.map((key) => ({
    name: t(`${key}.shortTitle`),
    url: `${SITE_URL}/${TOOLS.find((tool) => tool.key === key)!.path}`,
  }));

  const schemas = buildCategorySchema({
    name: tc(`${CATEGORY_KEY}.shortTitle`),
    description: tc(`${CATEGORY_KEY}.description`),
    path: PATH,
    tools: toolSchemas,
    faqItems: [1, 2, 3]
      .map((i) =>
        tc.has(`${CATEGORY_KEY}.faq${i}Q`)
          ? { q: tc(`${CATEGORY_KEY}.faq${i}Q`), a: tc(`${CATEGORY_KEY}.faq${i}A`) }
          : null
      )
      .filter(Boolean) as { q: string; a: string }[],
  });

  return (
    <>
      {schemas.map((s, i) => (
        <script
          key={i}
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(s) }}
        />
      ))}
      <CategoryPage categoryKey={CATEGORY_KEY} />
    </>
  );
}
```

- [ ] **Step 2: Create remaining 5 routes**

Each route follows the identical pattern — only `CATEGORY_KEY`, `PATH`, and the export name change. Create each file with the same body, substituting these values:

| File                           | CATEGORY_KEY   | PATH                     | Export name               |
| ------------------------------ | -------------- | ------------------------ | ------------------------- |
| `encoding-conversion/page.tsx` | `"encoding"`   | `"/encoding-conversion"` | `EncodingConversionRoute` |
| `security-crypto/page.tsx`     | `"security"`   | `"/security-crypto"`     | `SecurityCryptoRoute`     |
| `generators/page.tsx`          | `"generators"` | `"/generators"`          | `GeneratorsRoute`         |
| `visual-media/page.tsx`        | `"visual"`     | `"/visual-media"`        | `VisualMediaRoute`        |
| `reference-lookup/page.tsx`    | `"reference"`  | `"/reference-lookup"`    | `ReferenceLookupRoute`    |

The import path for CategoryPage uses `../../../components/category-page` — same 3-level relative path in all files.

- [ ] **Step 3: Verify build**

Run: `npx next build 2>&1 | tail -5`
Expected: Build succeeds (pages render with empty categories.json)

- [ ] **Step 4: Commit**

```bash
mkdir -p "app/[locale]/text-processing" "app/[locale]/encoding-conversion" "app/[locale]/security-crypto" "app/[locale]/generators" "app/[locale]/visual-media" "app/[locale]/reference-lookup"
git add "app/[locale]/text-processing/" "app/[locale]/encoding-conversion/" "app/[locale]/security-crypto/" "app/[locale]/generators/" "app/[locale]/visual-media/" "app/[locale]/reference-lookup/"
git commit -m "feat(seo): add 6 category aggregation route pages"
```

---

### Task 7: app/sitemap.ts — Add category pages

**Files:**

- Modify: `app/sitemap.ts`

- [ ] **Step 1: Add CATEGORY_SLUGS import and category page loop**

In `app/sitemap.ts`, add the import at the top:

```ts
import { TOOLS, CATEGORY_SLUGS } from "../libs/tools";
```

Then, after the tool pages loop (after line 46) and before the `staticPages` block, insert:

```ts
for (const locale of routing.locales) {
  const localePrefix = locale === defaultLocale ? "" : `/${locale}`;

  for (const slug of Object.values(CATEGORY_SLUGS)) {
    const path = `/${slug}`;
    urls.push({
      url: `${SITE_URL}${localePrefix}${path}`,
      lastModified: new Date(),
      changeFrequency: "weekly",
      priority: 0.7,
      alternates: makeAlternates(path),
    });
  }
}
```

- [ ] **Step 2: Verify build**

Run: `npx next build 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 3: Commit**

```bash
git add app/sitemap.ts
git commit -m "feat(seo): add 6 category pages to sitemap"
```

---

### Task 8: app/[locale]/home-page.tsx — Category headers become links

**Files:**

- Modify: `app/[locale]/home-page.tsx:207-245` (CategorySections component)

- [ ] **Step 1: Add imports**

In `home-page.tsx`, add `Link` and `CATEGORY_SLUGS` to the imports. Update the existing tools import line:

```tsx
import Link from "next/link";
import {
  getToolCards,
  getToolIconColor,
  getToolCardMap,
  getToolCardsByKeys,
  TOOL_CATEGORIES,
  QUICK_ACCESS_DEFAULT,
  CATEGORY_SLUGS,
} from "../../libs/tools";
```

Add `useLocale` to the next-intl import:

```tsx
import { useTranslations, useLocale } from "next-intl";
```

- [ ] **Step 2: Update CategorySections to accept locale**

The `CategorySections` component needs the locale prefix. Add `useLocale()` in the `HomeClient` component and pass it as a prop. In the `HomeClient` component, add:

```tsx
const locale = useLocale();
const prefix = locale === "en" ? "" : `/${locale}`;
```

Update the `CategorySections` props type to accept `localePrefix`:

```tsx
function CategorySections({
  categories,
  cardMap,
  onToolClick,
  categoryNames,
  localePrefix,
}: {
  categories: typeof TOOL_CATEGORIES;
  cardMap: Map<string, ReturnType<typeof getToolCards>[0]>;
  onToolClick: (path: string, key: string) => void;
  categoryNames: Record<string, string>;
  localePrefix: string;
}) {
```

- [ ] **Step 3: Replace span with Link in CategorySections**

In the `CategorySections` component, replace the category header `<span>` with a `<Link>`:

```tsx
<Link
  href={`${localePrefix}/${CATEGORY_SLUGS[cat.key]}`}
  className="text-sm font-medium text-fg-muted uppercase tracking-wider hover:text-accent-cyan transition-colors"
>
  {categoryNames[cat.key] ?? cat.key}
</Link>
```

- [ ] **Step 4: Pass localePrefix to CategorySections**

In `HomeClient`, update the `CategorySections` call to include the new prop:

```tsx
<CategorySections
  categories={TOOL_CATEGORIES}
  cardMap={cardMap}
  onToolClick={handleToolClick}
  categoryNames={categoryNames}
  localePrefix={prefix}
/>
```

- [ ] **Step 5: Verify build**

Run: `npx next build 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 6: Commit**

```bash
git add "app/[locale]/home-page.tsx"
git commit -m "feat(seo): make homepage category headers link to category pages"
```

---

## Phase 2: English Content + Tool Integration

### Task 9: en/categories.json — Full content

**Files:**

- Modify: `public/locales/en/categories.json`

- [ ] **Step 1: Write the complete en/categories.json**

Replace the placeholder `{}` with:

```json
{
  "text": {
    "title": "Text Processing Tools - JSON, Regex, Diff & More | OmniKit",
    "shortTitle": "Text Processing",
    "description": "Free online text processing tools for developers. Format JSON, test regex, compare diffs, convert cases, and more. All tools run 100% in your browser.",
    "intro": "A suite of 8 text processing tools designed for developers who work with structured and unstructured text daily. Format and validate JSON, test regular expressions with real-time matching, compare text differences, write Markdown with live preview, convert text cases, extract emails and URLs, count words and characters, and remove duplicate lines — all without sending any data to a server.",
    "faq1Q": "What text processing tools does OmniKit offer?",
    "faq1A": "OmniKit provides 8 text processing tools: JSON Formatter (with JSON5 support), Regex Tester, Text Diff, Markdown Editor, Text Case Converter, Email & URL Extractor, Word Counter, and Deduplicate Lines.",
    "faq2Q": "Are these text processing tools free?",
    "faq2A": "Yes, all OmniKit text processing tools are completely free and run entirely in your browser. No data is sent to any server.",
    "faq3Q": "Can I use these tools with large files?",
    "faq3A": "Yes. Most tools handle files up to several MB. JSON Formatter, Diff, and Markdown Editor use Web Workers for smooth performance on large inputs."
  },
  "encoding": {
    "title": "Encoding & Conversion Tools - Base64, URL, CSV, YAML | OmniKit",
    "shortTitle": "Encoding & Conversion",
    "description": "Free online encoding and conversion tools for developers. Base64 encode/decode, URL encode/decode, CSV conversion, YAML conversion, number base conversion, and more. 100% client-side.",
    "intro": "7 encoding and conversion tools that cover the most common data transformation needs. Encode and decode Base64 strings, URL-encode parameters, convert between JSON and CSV or YAML, transform number bases, and convert storage units. Every operation runs locally in your browser — no server uploads.",
    "faq1Q": "What encoding tools does OmniKit provide?",
    "faq1A": "OmniKit offers 7 encoding and conversion tools: Base64 Encoder/Decoder, URL Encoder/Decoder, JSON/CSV Converter, CSV/Markdown Table Converter, Number Base Converter, JSON/YAML Converter, and Storage Unit Converter.",
    "faq2Q": "Are the encoding conversions accurate?",
    "faq2A": "Yes. All conversions follow their respective RFC specifications (RFC 4648 for Base64, RFC 3986 for URL encoding) and produce standards-compliant output."
  },
  "security": {
    "title": "Security & Cryptography Tools - JWT, Hashing, Encryption | OmniKit",
    "shortTitle": "Security & Crypto",
    "description": "Free online security and cryptography tools for developers. Debug JWT tokens, generate hashes, create secure passwords, generate SSH keys, encrypt text, and calculate file checksums. 100% client-side.",
    "intro": "6 security and cryptography tools for developers. Debug and verify JWT tokens, generate MD5/SHA hashes, create cryptographically secure passwords, generate SSH key pairs, encrypt and decrypt text with AES/DES/RC4, and calculate file checksums. All cryptographic operations run entirely in your browser — your data never leaves your device.",
    "faq1Q": "Are the security tools safe to use with sensitive data?",
    "faq1A": "Yes. All security tools run 100% client-side using JavaScript cryptographic libraries. No data is ever transmitted to any server. You can verify this by checking the open-source code.",
    "faq2Q": "What encryption algorithms are supported?",
    "faq2A": "The Encrypt/Decrypt tool supports AES-256, DES, Triple DES, Rabbit, RC4, and RC4Drop. The Hash Generator supports MD5, SHA-1, SHA-224, SHA-256, SHA-384, SHA-512, SHA3, Keccak, and RIPEMD-160."
  },
  "generators": {
    "title": "Generator Tools - UUID, Cron, Timestamp, QR Code | OmniKit",
    "shortTitle": "Generators",
    "description": "Free online generator tools for developers. Generate UUIDs, build Cron expressions, convert Unix timestamps, and create QR codes. All tools run 100% in your browser.",
    "intro": "4 generator tools that help you create common developer artifacts. Generate UUID v4 and v7 identifiers, build and decode Cron expressions with next-run preview, convert Unix timestamps, and generate styled QR codes for text, URLs, WiFi, and more. Everything runs locally in your browser.",
    "faq1Q": "What can I generate with OmniKit?",
    "faq1A": "OmniKit provides 4 generator tools: UUID Generator (v4/v7), Cron Expression Generator, Unix Timestamp Converter, and QR Code Generator with custom styling.",
    "faq2Q": "Are the generated UUIDs unique?",
    "faq2A": "Yes. UUID v4 uses cryptographically secure random numbers, and UUID v7 combines timestamp with randomness. Both conform to RFC 4122/9562."
  },
  "visual": {
    "title": "Visual & Media Tools - Color Converter, Image Compressor | OmniKit",
    "shortTitle": "Visual & Media",
    "description": "Free online visual and media tools for developers. Convert colors between HEX/RGB/HSL/OKLCH, extract palettes from images, compress and convert images. 100% client-side.",
    "intro": "2 visual and media tools for design and image workflows. The Color Converter handles 7 color spaces (HEX, RGB, HSL, HSV, CMYK, LAB, OKLCH) with a visual picker, image palette extraction, WCAG contrast checking, and color-blindness simulation. The Image Compressor handles PNG, JPG, and WebP conversion with resize support. All processing runs locally.",
    "faq1Q": "What color spaces are supported?",
    "faq1A": "The Color Converter supports HEX, RGB, HSL, HSV, CMYK, LAB, and OKLCH with bidirectional conversion and visual comparison.",
    "faq2Q": "Is there a file size limit for image compression?",
    "faq2A": "No. Image compression runs entirely in your browser using Canvas API, so there is no server-side file size limit. Very large images may be slower to process."
  },
  "reference": {
    "title": "Reference & Lookup Tools - HTTP Status, ASCII, HTML Codes | OmniKit",
    "shortTitle": "Reference & Lookup",
    "description": "Free online reference and lookup tools for developers. HTTP status codes, ASCII table, HTML entities, SQLite viewer, and HTTP client. All tools run 100% in your browser.",
    "intro": "5 reference and lookup tools for quick developer lookups. Browse HTTP status codes with RFC references, look up ASCII character codes, encode HTML entities, explore SQLite databases with a SQL editor, and test REST APIs with the HTTP client. All tools are free and run entirely in your browser.",
    "faq1Q": "What reference tools does OmniKit provide?",
    "faq1A": "OmniKit offers 5 reference tools: HTTP Status Codes reference, HTTP Client for API testing, SQLite Database Viewer, ASCII Table, and HTML Entity Encoder.",
    "faq2Q": "Can I run SQL queries in the DB Viewer?",
    "faq2A": "Yes. The SQLite Viewer supports read-only SQL queries with autocomplete, query history, pagination, and export to CSV or JSON — all running locally in your browser via WebAssembly."
  }
}
```

- [ ] **Step 2: Verify build**

Run: `npx next build 2>&1 | tail -5`
Expected: Build succeeds

- [ ] **Step 3: Commit**

```bash
git add public/locales/en/categories.json
git commit -m "feat(seo): add English category page content"
```

---

### Task 10: tool page.tsx modification pattern

This task establishes the canonical pattern for modifying all 32 tool `page.tsx` files. The same code transformation is applied to each tool, with only the tool-specific values changing.

**Pattern for page.tsx modification:**

For each tool, three changes are needed in its `page.tsx`:

**A) Add imports** — Add after existing imports:

```tsx
import { TOOL_CATEGORIES, CATEGORY_SLUGS } from "../../../libs/tools";
```

**B) Add category translations + compute category** — In the default export function, after the existing tool-specific namespace translation (e.g., `const tx = ...`), add:

```tsx
const tc = await getTranslations({ locale, namespace: "categories" });
const category = TOOL_CATEGORIES.find((c) => c.tools.includes(TOOL_KEY))!;
const categorySlug = CATEGORY_SLUGS[category.key];
```

Where `TOOL_KEY` is a new const added after `PATH`:

```tsx
const TOOL_KEY = "json";
```

**C) Update buildToolSchemas call** — Replace `description: t("json.description")` with the aeo-preferring version and add category params:

```tsx
const schemas = buildToolSchemas({
  name: t("json.title"),
  description: tx.has("descriptions.aeoDefinition")
    ? tx("descriptions.aeoDefinition")
    : t("json.description"),
  path: PATH,
  categoryName: tc(`${category.key}.shortTitle`),
  categoryPath: `/${categorySlug}`,
  faqItems: [1, 2, 3].map((i) => ({
    q: tx(`descriptions.faq${i}Q`),
    a: tx(`descriptions.faq${i}A`),
  })),
});
```

Note: The tool-specific namespace variable name (`tx`, `tb`, `tp`, `ts`) varies per tool — see the Per-Tool Reference Table at the top of this plan.

**Example: json/page.tsx after modification:**

```tsx
import { getTranslations } from "next-intl/server";
import { generatePageMeta } from "../../../libs/seo";
import { buildToolSchemas } from "../../../components/json-ld";
import { TOOL_CATEGORIES, CATEGORY_SLUGS } from "../../../libs/tools";
import JsonPage from "./json-page";

const PATH = "/json";
const TOOL_KEY = "json";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  return generatePageMeta({
    locale,
    path: PATH,
    title: t("json.title"),
    description: t("json.description"),
  });
}

export default async function JsonRoute({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  const tx = await getTranslations({ locale, namespace: "json" });
  const tc = await getTranslations({ locale, namespace: "categories" });
  const category = TOOL_CATEGORIES.find((c) => c.tools.includes(TOOL_KEY))!;
  const categorySlug = CATEGORY_SLUGS[category.key];
  const schemas = buildToolSchemas({
    name: t("json.title"),
    description: tx.has("descriptions.aeoDefinition")
      ? tx("descriptions.aeoDefinition")
      : t("json.description"),
    path: PATH,
    categoryName: tc(`${category.key}.shortTitle`),
    categoryPath: `/${categorySlug}`,
    faqItems: [1, 2, 3].map((i) => ({
      q: tx(`descriptions.faq${i}Q`),
      a: tx(`descriptions.faq${i}A`),
    })),
  });

  return (
    <>
      {schemas.map((s, i) => (
        <script
          key={i}
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(s) }}
        />
      ))}
      <JsonPage />
    </>
  );
}
```

**Apply this pattern to all 32 tools.** Per-tool values:

| Tool        | TOOL_KEY      | PATH           | Namespace Var | faqItems range                   |
| ----------- | ------------- | -------------- | ------------- | -------------------------------- |
| json        | "json"        | "/json"        | tx            | 1..3                             |
| regex       | "regex"       | "/regex"       | tx            | 1..3                             |
| diff        | "diff"        | "/diff"        | tx            | 1..3                             |
| markdown    | "markdown"    | "/markdown"    | tx            | 1..3                             |
| textcase    | "textcase"    | "/textcase"    | tx            | 1..3                             |
| extractor   | "extractor"   | "/extractor"   | tx            | 1..3                             |
| wordcounter | "wordcounter" | "/wordcounter" | tx            | 1..3                             |
| deduplines  | "deduplines"  | "/deduplines"  | tx            | 1..3                             |
| base64      | "base64"      | "/base64"      | tb            | has howToSteps 1..4, no faqItems |
| urlencoder  | "urlencoder"  | "/urlencoder"  | tx            | 1..3                             |
| csv         | "csv"         | "/csv"         | tx            | 1..3                             |
| csv-md      | "csv-md"      | "/csv-md"      | tx            | 1..3                             |
| numbase     | "numbase"     | "/numbase"     | tx            | 1..3                             |
| yaml        | "yaml"        | "/yaml"        | tx            | 1..3                             |
| storageunit | "storageunit" | "/storageunit" | tx            | 1..3                             |
| jwt         | "jwt"         | "/jwt"         | tx            | 1..3                             |
| hashing     | "hashing"     | "/hashing"     | tx            | 1..3                             |
| password    | "password"    | "/password"    | tp            | 1..5 faq + 1..5 howToSteps       |
| sshkey      | "sshkey"      | "/sshkey"      | ts            | 1..4 faq + 1..4 howToSteps       |
| cipher      | "cipher"      | "/cipher"      | tx            | 1..3                             |
| checksum    | "checksum"    | "/checksum"    | tx            | 1..3                             |
| uuid        | "uuid"        | "/uuid"        | tx            | 1..3                             |
| cron        | "cron"        | "/cron"        | tx            | 1..3                             |
| unixtime    | "unixtime"    | "/unixtime"    | tx            | 1..3                             |
| qrcode      | "qrcode"      | "/qrcode"      | tx            | 1..3                             |
| color       | "color"       | "/color"       | tx            | 1..3                             |
| image       | "image"       | "/image"       | tx            | 1..3                             |
| httpstatus  | "httpstatus"  | "/httpstatus"  | tx            | 1..3                             |
| httpclient  | "httpclient"  | "/httpclient"  | tx            | 1..3                             |
| dbviewer    | "dbviewer"    | "/dbviewer"    | tx            | 1..3                             |
| ascii       | "ascii"       | "/ascii"       | tx            | 1..3                             |
| htmlcode    | "htmlcode"    | "/htmlcode"    | tx            | 1..3                             |

**Important notes per tool:**

- `base64/page.tsx`: Uses `howToSteps` not `faqItems`. Keep existing `howToSteps` and add `categoryName`/`categoryPath`/`aeoDefinition` only.
- `password/page.tsx`: Uses both `faqItems` (1..5) and `howToSteps` (1..5). Keep both, add the 3 new fields.
- `sshkey/page.tsx`: Uses both `faqItems` (1..4) and `howToSteps` (1..4). Keep both, add the 3 new fields.

- [ ] **Apply to all 32 tools, then commit:**

```bash
git add "app/[locale]/*/page.tsx"
git commit -m "feat(seo): pass category info + aeoDefinition to buildToolSchemas for all tools"
```

---

### Task 11: tool \*-page.tsx modification pattern (AEO paragraph + category badge + renderLinkedText)

This task establishes the canonical pattern for modifying all 32 tool `*-page.tsx` Description functions.

**Pattern for \*-page.tsx modification:**

For each tool's `*-page.tsx`, four changes are needed:

**A) Add imports** at the top of the file:

```tsx
import Link from "next/link";
import { useLocale } from "next-intl";
import { Badge } from "../../../components/ui/badge";
import { CATEGORY_SLUGS } from "../../../libs/tools";
import { renderLinkedText } from "../../../utils/linked-text";
```

Note: `useLocale` may need to be merged into the existing `import { useTranslations } from "next-intl"` line. Some tools already import `useLocale` — check before adding.

**B) Add hooks in Description function** — After existing translation hooks:

```tsx
const tt = useTranslations("tools");
const locale = useLocale();
const prefix = locale === "en" ? "" : `/${locale}`;
const categoryKey = "text";
const categorySlug = CATEGORY_SLUGS[categoryKey];
```

Where `categoryKey` is the category this tool belongs to (see Per-Tool Reference Table). Use `tt` (tools namespace) since most tools already use `tc` for "common".

Note: If the tool already has a `tt` variable, use `ttools` instead. Check the existing Description function first.

**C) Add AEO paragraph + category badge** — As the first elements inside the `<section id="description">`, before the first `<div className="mb-4">` or `<h2>`:

```tsx
<p className="text-fg-primary text-sm leading-relaxed font-medium">
  {t("descriptions.aeoDefinition")}
</p>
<div className="mt-2 mb-4">
  <Link href={`${prefix}/${categorySlug}`}>
    <Badge>{tt(`categories.${categoryKey}`)}</Badge>
  </Link>
</div>
```

**D) Wrap selected paragraphs with renderLinkedText** — For paragraphs that contain `[text](/path)` link syntax, change:

```tsx
<p>{t("descriptions.whatIsP1")}</p>
```

to:

```tsx
<p>{renderLinkedText(t("descriptions.whatIsP1"), locale)}</p>
```

Only paragraphs listed in the "Link paragraphs" column below get this treatment. All other paragraphs remain unchanged.

**Per-tool values for \*-page.tsx:**

| Tool        | Category Key | Link Paragraphs                                 |
| ----------- | ------------ | ----------------------------------------------- |
| json        | text         | whatIsP1, useCasesP2                            |
| regex       | text         | whatIsP1                                        |
| diff        | text         | whatIsP1, useCasesP1                            |
| markdown    | text         | whatIsP1                                        |
| textcase    | text         | (none)                                          |
| extractor   | text         | (none)                                          |
| wordcounter | text         | whatIsP1                                        |
| deduplines  | text         | whatIsP1                                        |
| base64      | encoding     | whatIsP1, useCasesP1                            |
| urlencoder  | encoding     | whatIsP1                                        |
| csv         | encoding     | whatIsP1, useCasesP1                            |
| csv-md      | encoding     | whatIsP1                                        |
| numbase     | encoding     | whatIs (in descriptions)                        |
| yaml        | encoding     | whatIsP1, useCasesP1                            |
| storageunit | encoding     | whatIsP1                                        |
| jwt         | security     | whatIs (in descriptions)                        |
| hashing     | security     | (none)                                          |
| password    | security     | (none)                                          |
| sshkey      | security     | (none)                                          |
| cipher      | security     | (none)                                          |
| checksum    | security     | (none)                                          |
| uuid        | generators   | whatIsP1                                        |
| cron        | generators   | whatIs (in descriptions)                        |
| unixtime    | generators   | (none — description.whatIs in singular section) |
| qrcode      | generators   | (none — description.intro in singular section)  |
| color       | visual       | description.introP1 (singular section)          |
| image       | visual       | descriptions.p1                                 |
| httpstatus  | reference    | description.text (singular section)             |
| httpclient  | reference    | description.text (singular section)             |
| dbviewer    | reference    | whatIsP1                                        |
| ascii       | reference    | whatIsP1                                        |
| htmlcode    | reference    | description.p1 (singular section)               |

**Example: json/json-page.tsx Description function after modification:**

```tsx
function Description() {
  const t = useTranslations("json");
  const tc = useTranslations("common");
  const tt = useTranslations("tools");
  const locale = useLocale();
  const prefix = locale === "en" ? "" : `/${locale}`;
  const categoryKey = "text";
  const categorySlug = CATEGORY_SLUGS[categoryKey];

  const faqItems = [1, 2, 3].map((i) => ({
    title: t(`descriptions.faq${i}Q`),
    content: <p>{t(`descriptions.faq${i}A`)}</p>,
  }));
  return (
    <section id="description" className="mt-8">
      <p className="text-fg-primary text-sm leading-relaxed font-medium">
        {t("descriptions.aeoDefinition")}
      </p>
      <div className="mt-2 mb-4">
        <Link href={`${prefix}/${categorySlug}`}>
          <Badge>{tt(`categories.${categoryKey}`)}</Badge>
        </Link>
      </div>
      <div className="mb-4">
        <h2 className="font-semibold text-fg-primary text-base">{t("descriptions.whatIsTitle")}</h2>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{renderLinkedText(t("descriptions.whatIsP1"), locale)}</p>
          <p>{t("descriptions.whatIsP2")}</p>
          <p>{t("descriptions.whatIsP3")}</p>
        </div>
      </div>

      <div className="mb-4">
        <h2 className="font-semibold text-fg-primary text-base">{t("descriptions.json5Title")}</h2>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{t("descriptions.json5P1")}</p>
          <p>{t("descriptions.json5P2")}</p>
        </div>
      </div>

      <div className="mb-4">
        <h2 className="font-semibold text-fg-primary text-base">
          {t("descriptions.useCasesTitle")}
        </h2>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{t("descriptions.useCasesP1")}</p>
          <p>{renderLinkedText(t("descriptions.useCasesP2"), locale)}</p>
          <p>{t("descriptions.useCasesP3")}</p>
          <p>{t("descriptions.useCasesP4")}</p>
        </div>
      </div>

      <div className="mb-4">
        <h2 className="font-semibold text-fg-primary text-base">
          {t("descriptions.limitationsTitle")}
        </h2>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{t("descriptions.limitationsP1")}</p>
          <p>{t("descriptions.limitationsP2")}</p>
          <p>{t("descriptions.limitationsP3")}</p>
        </div>
      </div>
      <div className="mt-8">
        <div className="flex items-center gap-2 mb-4">
          <CircleHelp size={16} className="text-accent-cyan shrink-0" aria-hidden="true" />
          <h2 className="font-semibold text-fg-primary text-base text-pretty">
            {t("descriptions.faqTitle")}
          </h2>
        </div>
        <Accordion items={faqItems} />
      </div>
    </section>
  );
}
```

**Apply this pattern to all 32 tools.** Each tool's Description function structure is different, but the insertion point (AEO paragraph + badge before the first content block) and paragraph wrapping follow the same logic.

- [ ] **Apply to all 32 tools, then commit:**

```bash
git add "app/[locale]/*/*-page.tsx"
git commit -m "feat(seo): add AEO paragraph, category badge, renderLinkedText to all tools"
```

---

### Task 12: English locale files — text category (8 tools)

**Files:**

- Modify: `public/locales/en/json.json`
- Modify: `public/locales/en/regex.json`
- Modify: `public/locales/en/diff.json`
- Modify: `public/locales/en/markdown.json`
- Modify: `public/locales/en/textcase.json`
- Modify: `public/locales/en/extractor.json`
- Modify: `public/locales/en/wordcounter.json`
- Modify: `public/locales/en/deduplines.json`

For each tool, add `"aeoDefinition"` inside the `"descriptions"` object, and modify link paragraphs to include `[text](/path)` syntax.

- [ ] **Step 1: json.json — Add aeoDefinition + modify whatIsP1, useCasesP2**

Inside the `descriptions` object, add as the first key:

```json
"aeoDefinition": "JSON Formatter is a free online tool that formats, validates, and minifies JSON data instantly in your browser. Supports JSON5, configurable indentation, and key sorting. No data is sent to any server.",
```

Modify `whatIsP1` to include links to CSV and YAML:

```json
"whatIsP1": "JSON (JavaScript Object Notation) is the universal data interchange format of the web. APIs return it, config files use it, and every modern programming language can parse it. Pair it with the [YAML converter](/yaml) or [CSV converter](/csv) for cross-format workflows. Formatting (pretty-printing) takes minified, hard-to-read JSON and adds indentation and line breaks so humans can scan and understand the structure at a glance.",
```

Modify `useCasesP2` to include a link:

```json
"useCasesP2": "Minifying config files: compress JSON before deploying to production to reduce file size. Use [Text Diff](/diff) to verify the minified output matches the original.",
```

- [ ] **Step 2: regex.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "Regex Tester is a free online tool for testing regular expressions with real-time match highlighting, capture group inspection, and replacement preview. Includes a built-in pattern library and token-level explanation. Runs entirely in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "A regex (regular expression) tester is an interactive tool that lets you build and test regex patterns against sample text in real time. Use it alongside [JSON formatter](/json) for data validation or [Text Case Converter](/textcase) for batch text transformations.",
```

- [ ] **Step 3: diff.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "Text Diff is a free online tool for comparing two texts with line-level and word-level precision. Side-by-side or inline view with highlighted changes. Runs entirely in your browser — no data is sent to any server.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "A diff is a structured comparison between two pieces of text that highlights what was added, what was removed, and what stayed the same. Developers use it every day to review code changes, audit configuration drift, and compare [JSON](/json) or [CSV](/csv) files.",
```

Modify `useCasesP1`:

```json
"useCasesP1": "Comparing two configuration files ([JSON](/json), [YAML](/yaml), .env) to spot drift between environments.",
```

- [ ] **Step 4: markdown.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "Markdown Editor is a free online tool for writing and previewing Markdown with GitHub Flavored Markdown support. Includes syntax highlighting, live preview, and PDF/PNG export. Runs entirely in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "Markdown is a lightweight markup language that lets you format plain text using simple, readable syntax. It's the de-facto standard for READMEs, documentation, and notes — easy to write, easy to [diff](/diff), and renders beautifully across platforms.",
```

- [ ] **Step 5: textcase.json — Add aeoDefinition**

```json
"aeoDefinition": "Text Case Converter is a free online tool that converts text between camelCase, PascalCase, snake_case, kebab-case, CONSTANT_CASE, and more. Instant conversion with real-time preview. No data leaves your browser.",
```

No link paragraphs for textcase.

- [ ] **Step 6: extractor.json — Add aeoDefinition**

```json
"aeoDefinition": "Email & URL Extractor is a free online tool that extracts emails, URLs, and phone numbers from any text instantly. Supports multiple formats and batch processing. Runs entirely in your browser.",
```

No link paragraphs for extractor.

- [ ] **Step 7: wordcounter.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "Word Counter is a free online tool for real-time text analysis including word count, character count, sentence count, reading time, and keyword density. Supports CJK text. No data is sent to any server.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "The Word Counter is a free online tool that provides real-time text analysis including word count, character count, sentence count, paragraph count, and reading time estimation. Pair it with the [Text Case Converter](/textcase) for batch text transformations or [Deduplicate Lines](/deduplines) to clean up lists.",
```

- [ ] **Step 8: deduplines.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "Deduplicate Lines is a free online tool that removes duplicate lines from text while preserving order. Supports case-sensitive matching, whitespace trimming, and empty line removal. Runs entirely in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "Line deduplication removes duplicate lines from text, keeping only the first occurrence of each unique line. The original order is preserved. Use the [Email Extractor](/extractor) to pull unique addresses, or [Word Counter](/wordcounter) to analyze the cleaned output.",
```

- [ ] **Step 9: Commit**

```bash
git add public/locales/en/json.json public/locales/en/regex.json public/locales/en/diff.json public/locales/en/markdown.json public/locales/en/textcase.json public/locales/en/extractor.json public/locales/en/wordcounter.json public/locales/en/deduplines.json
git commit -m "feat(seo): add en aeoDefinition + inline links for text category tools"
```

---

### Task 13: English locale files — encoding category (7 tools)

**Files:**

- Modify: `public/locales/en/base64.json`
- Modify: `public/locales/en/urlencoder.json`
- Modify: `public/locales/en/csv.json`
- Modify: `public/locales/en/csv-md.json`
- Modify: `public/locales/en/numbase.json`
- Modify: `public/locales/en/yaml.json`
- Modify: `public/locales/en/storageunit.json`

- [ ] **Step 1: base64.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "Base64 Encoder/Decoder is a free online tool for encoding and decoding Base64 strings. Supports Basic Authentication headers, multiple charsets, and real-time conversion. All processing runs in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "Base64 encoding is a way to convert data (typically binary) into the ASCII character set. It is important to mention here that Base64 is not an encryption or compression technique, although it can sometimes be confused as encryption due to the way it seems to obscure data. Use [Hashing](/hashing) for cryptographic purposes or [URL Encoder](/urlencoder) for URL-safe strings.",
```

Modify `useCasesP1`:

```json
"useCasesP1": "You can also use Base64 to represent binary data in a way that is compatible with HTML, JavaScript, and CSS. For example, you can embed an image inline in a CSS or JavaScript file using Base64. The [Cipher](/cipher) tool uses Base64 as the default output format for encrypted text.",
```

- [ ] **Step 2: urlencoder.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "URL Encoder/Decoder is a free online tool for encoding and decoding URL strings. Supports Component, Whole URL, and Form encoding modes. Runs entirely in your browser — no data is sent to any server.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "URL encoding (also called percent-encoding) is the mechanism defined by RFC 3986 for carrying data inside URLs. Characters that have reserved meaning in a URL (such as ? / # & =) or that lie outside the ASCII printable range are rewritten as one or more %XX triplets, where XX is the byte's hexadecimal value. See also [Base64](/base64) for binary-to-text encoding.",
```

- [ ] **Step 3: csv.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "CSV Converter is a free online tool for converting between JSON and CSV formats with real-time preview. Bidirectional conversion with proper escaping and Unicode support. No data leaves your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "CSV (Comma-Separated Values) is a plain text format for storing tabular data. Each line represents a row, and fields are separated by commas. It's the most common format for data exchange between spreadsheets, databases, and applications. Use the [JSON formatter](/json) to inspect source data, or [YAML converter](/yaml) for YAML output.",
```

Modify `useCasesP1`:

```json
"useCasesP1": "Importing/exporting data between spreadsheets and web APIs, converting [JSON](/json) API responses to CSV for analysis, preparing data for database bulk import with [DB Viewer](/dbviewer), sharing tabular data in a universally readable format.",
```

- [ ] **Step 4: csv-md.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "CSV ↔ Markdown Table Converter is a free online tool for converting between CSV and Markdown table formats. Real-time bidirectional conversion. Runs entirely in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "Convert between CSV (Comma-Separated Values) and Markdown Table formats. CSV is the standard for spreadsheet data exchange, while Markdown tables are commonly used in documentation, README files, and GitHub issues. Use the [CSV converter](/csv) for JSON↔CSV, or [Markdown editor](/markdown) to edit Markdown files.",
```

- [ ] **Step 5: numbase.json — Add aeoDefinition + modify whatIs**

```json
"aeoDefinition": "Number Base Converter is a free online tool for converting numbers between binary, octal, decimal, and hexadecimal. Supports 8/16/32/64-bit two's complement and a visual bit editor. Runs entirely in your browser.",
```

Modify `whatIs` (note: key is `whatIs`, not `whatIsP1`):

```json
"whatIs": "This tool converts numbers between binary (base 2), octal (base 8), decimal (base 10), and hexadecimal (base 16) in real time. It also supports 8/16/32/64-bit two's complement representation with a visual bit editor. Related: [Color converter](/color) for hex color codes, [Storage unit converter](/storageunit) for byte calculations.",
```

- [ ] **Step 6: yaml.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "YAML Converter is a free online tool for converting between JSON and YAML formats. Supports YAML 1.2, multi-document, anchors, and aliases. All processing runs in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "YAML (YAML Ain't Markup Language) is a human-readable data serialization format commonly used for configuration files, data exchange, and structured content. It uses indentation to denote structure, making it more readable than [JSON](/json) for complex nested data.",
```

Modify `useCasesP1`:

```json
"useCasesP1": "Configuration files (Docker Compose, Kubernetes, CI/CD pipelines), API definitions (OpenAPI/Swagger), data serialization, log file formats, and cross-language data exchange. Convert to [CSV](/csv) for spreadsheet import, or compare versions with [Text Diff](/diff).",
```

- [ ] **Step 7: storageunit.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "Storage Unit Converter is a free online tool for converting between bytes, KB, MB, GB, TB, and PB. Supports both SI (decimal) and IEC (binary) standards. Runs entirely in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "The Storage Unit Converter lets you instantly convert between digital storage units — bytes, KB, MB, GB, TB, PB — supporting both decimal (SI) and binary (IEC) standards. See also [Number Base converter](/numbase) for binary/hex conversions, or [File Checksum](/checksum) to verify file sizes.",
```

- [ ] **Step 8: Commit**

```bash
git add public/locales/en/base64.json public/locales/en/urlencoder.json public/locales/en/csv.json public/locales/en/csv-md.json public/locales/en/numbase.json public/locales/en/yaml.json public/locales/en/storageunit.json
git commit -m "feat(seo): add en aeoDefinition + inline links for encoding category tools"
```

---

### Task 14: English locale files — security category (6 tools)

**Files:**

- Modify: `public/locales/en/jwt.json`
- Modify: `public/locales/en/hashing.json`
- Modify: `public/locales/en/password.json`
- Modify: `public/locales/en/sshkey.json`
- Modify: `public/locales/en/cipher.json`
- Modify: `public/locales/en/checksum.json`

- [ ] **Step 1: jwt.json — Add aeoDefinition + modify whatIs**

```json
"aeoDefinition": "JWT Debugger is a free online tool for encoding, decoding, and verifying JSON Web Tokens. Supports HS256, RS256, ES256 and more algorithms. All processing runs in your browser — no tokens are sent to any server.",
```

Modify `whatIs` (key name varies — check file, it may be inside `descriptions`):

```json
"whatIs": "JSON Web Token (JWT) is an open standard (RFC 7519) for securely transmitting information between parties as a JSON object. JWTs are commonly used for authentication and authorization in web applications. Debug tokens here, then verify payloads with [Base64](/base64) decoding or check key integrity with [Hashing](/hashing).",
```

- [ ] **Step 2: hashing.json — Add aeoDefinition**

```json
"aeoDefinition": "Hash Generator is a free online tool for generating MD5, SHA-1, SHA-256, SHA-512, SHA3, Keccak, and RIPEMD-160 hashes. Supports HMAC and file hashing. No data is sent to any server.",
```

No link paragraphs (hmac content is specialized).

- [ ] **Step 3: password.json — Add aeoDefinition**

```json
"aeoDefinition": "Password Generator is a free online tool for creating cryptographically secure passwords. Supports random, memorable, and PIN generation with a built-in strength checker. Runs entirely in your browser.",
```

No link paragraphs (step-based content).

- [ ] **Step 4: sshkey.json — Add aeoDefinition**

```json
"aeoDefinition": "SSH Key Generator is a free online tool for generating RSA and Ed25519 SSH key pairs entirely in your browser. Supports passphrase encryption and fingerprint display. No data is sent to any server.",
```

No link paragraphs (step-based content).

- [ ] **Step 5: cipher.json — Add aeoDefinition**

```json
"aeoDefinition": "Encrypt/Decrypt is a free online tool for encrypting and decrypting text using AES, DES, Triple DES, Rabbit, RC4, and RC4Drop. All processing runs in your browser — no data is sent to any server.",
```

No link paragraphs (algorithm descriptions).

- [ ] **Step 6: checksum.json — Add aeoDefinition**

```json
"aeoDefinition": "File Checksum is a free online tool for calculating MD5, SHA-1, SHA-256, and SHA-512 checksums for any file. Supports unlimited file size with 100% client-side processing.",
```

No link paragraphs (FAQ-only content).

- [ ] **Step 7: Commit**

```bash
git add public/locales/en/jwt.json public/locales/en/hashing.json public/locales/en/password.json public/locales/en/sshkey.json public/locales/en/cipher.json public/locales/en/checksum.json
git commit -m "feat(seo): add en aeoDefinition for security category tools"
```

---

### Task 15: English locale files — generators category (4 tools)

**Files:**

- Modify: `public/locales/en/uuid.json`
- Modify: `public/locales/en/cron.json`
- Modify: `public/locales/en/unixtime.json`
- Modify: `public/locales/en/qrcode.json`

- [ ] **Step 1: uuid.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "UUID Generator is a free online tool for generating UUID v4 (random) and v7 (time-ordered) identifiers in bulk. Conforms to RFC 4122/9562. Runs entirely in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "UUID (Universally Unique Identifier) is a 128-bit identifier standard defined in RFC 4122 and RFC 9562. This tool generates UUID v4 (random) and v7 (time-ordered) in bulk. Use generated IDs as [Password](/password) seeds, encode them in [QR codes](/qrcode), or verify uniqueness with [Hashing](/hashing).",
```

- [ ] **Step 2: cron.json — Add aeoDefinition + modify whatIs**

```json
"aeoDefinition": "Cron Expression Generator is a free online tool for building and decoding Cron expressions. Supports Standard, Spring, and Quartz modes with human-readable explanations and next-run preview. No data leaves your browser.",
```

Modify `whatIs`:

```json
"whatIs": "Cron is a time-based job scheduler in Unix-like operating systems. A Cron expression is a string of five or six fields that defines when a job should run. This tool supports Standard, Spring, and Quartz dialects. Pair it with the [Unix Timestamp](/unixtime) converter for time calculations.",
```

- [ ] **Step 3: unixtime.json — Add aeoDefinition**

```json
"aeoDefinition": "Unix Timestamp Converter is a free online tool for converting between Unix timestamps and human-readable dates. Supports seconds and milliseconds with live clock. Runs entirely in your browser.",
```

No link paragraphs (unixtime has a split `description`/`descriptions` structure — the `whatIs` is in the singular `description` section which is rendered differently).

- [ ] **Step 4: qrcode.json — Add aeoDefinition**

```json
"aeoDefinition": "QR Code Generator is a free online tool for creating QR codes for text, URLs, WiFi, vCard, and more. Supports custom colors, logos, and dot styles with SVG/PNG export. No data is sent to any server.",
```

No link paragraphs (qrcode has a split `description`/`descriptions` structure).

- [ ] **Step 5: Commit**

```bash
git add public/locales/en/uuid.json public/locales/en/cron.json public/locales/en/unixtime.json public/locales/en/qrcode.json
git commit -m "feat(seo): add en aeoDefinition + inline links for generator category tools"
```

---

### Task 16: English locale files — visual category (2 tools)

**Files:**

- Modify: `public/locales/en/color.json`
- Modify: `public/locales/en/image.json`

- [ ] **Step 1: color.json — Add aeoDefinition**

Add inside the `descriptions` object (the FAQ section):

```json
"aeoDefinition": "Color Converter is a free online tool for converting between HEX, RGB, HSL, OKLCH and other color spaces. Includes a visual picker, image palette extraction, WCAG contrast checker, and color-blindness preview. Runs entirely in your browser.",
```

Note: color has split `description`/`descriptions`. The aeoDefinition goes in `descriptions` (the FAQ section). For the link paragraph, modify `description.introP1` in the singular section. This is accessed via `t("description.introP1")` in the \*-page.tsx, so the renderLinkedText wrapping applies there.

Modify `description.introP1`:

```json
"introP1": "This color tool converts between 7 color spaces, generates harmonies, checks contrast ratios (WCAG and APCA), simulates color blindness, and extracts palettes from images. See [Number Base converter](/numbase) for hex calculations, or [Image compressor](/image) for image processing.",
```

- [ ] **Step 2: image.json — Add aeoDefinition + modify descriptions.p1**

```json
"aeoDefinition": "Image Compressor is a free online tool for compressing, resizing, and converting images between PNG, JPG, and WebP formats. All processing runs locally in your browser.",
```

Modify `descriptions.p1`:

```json
"p1": "Compress, resize, and convert images between PNG, JPG, and WebP formats directly in your browser. Extract dominant colors with the [Color converter](/color), or verify image integrity with [File Checksum](/checksum).",
```

- [ ] **Step 3: Commit**

```bash
git add public/locales/en/color.json public/locales/en/image.json
git commit -m "feat(seo): add en aeoDefinition + inline links for visual category tools"
```

---

### Task 17: English locale files — reference category (5 tools)

**Files:**

- Modify: `public/locales/en/httpstatus.json`
- Modify: `public/locales/en/httpclient.json`
- Modify: `public/locales/en/dbviewer.json`
- Modify: `public/locales/en/ascii.json`
- Modify: `public/locales/en/htmlcode.json`

- [ ] **Step 1: httpstatus.json — Add aeoDefinition + modify description.text**

```json
"aeoDefinition": "HTTP Status Codes is a free online reference for all HTTP status codes with descriptions, categories, and RFC links. Covers official IANA codes and common unofficial codes.",
```

Modify `description.text`:

```json
"text": "A complete reference of HTTP status codes with descriptions, categories, and RFC specification links. Covers all IANA-registered codes plus common unofficial codes. Test APIs with the [HTTP Client](/httpclient), or encode URLs with the [URL Encoder](/urlencoder).",
```

- [ ] **Step 2: httpclient.json — Add aeoDefinition + modify description.text**

```json
"aeoDefinition": "HTTP Client is a free online tool for sending HTTP requests and testing REST APIs directly in your browser. Supports GET, POST, PUT, DELETE with headers, body, and auth. No data is sent through any server.",
```

Modify `description.text`:

```json
"text": "Send HTTP requests and test REST APIs directly in your browser. Supports all common methods with headers, body, and authentication. Look up status codes in the [HTTP Status](/httpstatus) reference, or encode query parameters with the [URL Encoder](/urlencoder).",
```

- [ ] **Step 3: dbviewer.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "SQLite Viewer is a free online tool for opening and exploring SQLite database files in your browser. Run SQL queries with autocomplete, browse tables, and export to CSV or JSON. No data leaves your device.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "The SQLite Viewer lets you open and explore SQLite database files directly in your browser — no server upload required. Run SQL queries, browse tables, and export data to [CSV](/csv) or [JSON](/json). Powered by sql.js (WebAssembly).",
```

- [ ] **Step 4: ascii.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "ASCII Table is a free online reference for ASCII characters with hex, octal, decimal, and HTML codes. Includes search, filtering, and conversion tools. Runs entirely in your browser.",
```

Modify `whatIsP1`:

```json
"whatIsP1": "ASCII (American Standard Code for Information Interchange) is a character encoding standard that assigns numeric values to letters, digits, punctuation, and control codes. It covers 128 characters from 0 to 127. See [HTML entities](/htmlcode) for web-safe characters, or [Number Base converter](/numbase) for hex/binary conversions.",
```

- [ ] **Step 5: htmlcode.json — Add aeoDefinition + modify description.p1**

```json
"aeoDefinition": "HTML Entity Encoder is a free online tool for encoding and decoding HTML special characters and entities. Complete reference with numeric and named entities. Runs entirely in your browser.",
```

Modify `description.p1`:

```json
"p1": "HTML entities are special codes used to represent reserved characters in HTML. For example, &lt; for <, &amp; for &, and &quot; for \". See the [ASCII table](/ascii) for character codes, or [HTTP status codes](/httpstatus) for web response references.",
```

- [ ] **Step 6: Commit**

```bash
git add public/locales/en/httpstatus.json public/locales/en/httpclient.json public/locales/en/dbviewer.json public/locales/en/ascii.json public/locales/en/htmlcode.json
git commit -m "feat(seo): add en aeoDefinition + inline links for reference category tools"
```

---

## Phase 3: zh-CN Content

### Task 18: zh-CN/categories.json — Full content

**Files:**

- Modify: `public/locales/zh-CN/categories.json`

- [ ] **Step 1: Write the complete zh-CN/categories.json**

Replace the placeholder `{}` with:

```json
{
  "text": {
    "title": "文本处理工具 - JSON、正则、文本对比等 | OmniKit",
    "shortTitle": "文本处理",
    "description": "面向开发者的免费在线文本处理工具。格式化 JSON、测试正则表达式、对比文本差异、转换大小写等。所有工具完全在浏览器中运行。",
    "intro": "一套包含 8 个文本处理工具的开发者工具集。格式化和验证 JSON、实时测试正则表达式、对比文本差异、Markdown 实时预览编辑、文本大小写转换、提取邮箱和 URL、字数统计、去除重复行——所有操作均在本地完成，不会上传任何数据。",
    "faq1Q": "OmniKit 提供哪些文本处理工具？",
    "faq1A": "OmniKit 提供 8 个文本处理工具：JSON 格式化（支持 JSON5）、正则测试器、文本对比、Markdown 编辑器、文本大小写转换、邮箱/URL 提取器、字数统计和去重工具。",
    "faq2Q": "这些文本处理工具免费吗？",
    "faq2A": "是的，所有 OmniKit 文本处理工具完全免费，且所有操作均在浏览器中完成，不会将任何数据发送到服务器。",
    "faq3Q": "可以处理大文件吗？",
    "faq3A": "可以。大多数工具支持处理数 MB 的文件。JSON 格式化、文本对比和 Markdown 编辑器使用 Web Worker 确保大文件处理时的流畅体验。"
  },
  "encoding": {
    "title": "编码转换工具 - Base64、URL、CSV、YAML | OmniKit",
    "shortTitle": "编码转换",
    "description": "面向开发者的免费在线编码转换工具。Base64 编解码、URL 编解码、CSV 转换、YAML 转换、进制转换等。100% 浏览器端处理。",
    "intro": "7 个编码转换工具，覆盖最常见的数据转换需求。Base64 编解码、URL 编码、JSON 与 CSV/YAML 互转、数字进制转换、存储单位换算。所有操作均在浏览器本地运行。",
    "faq1Q": "OmniKit 提供哪些编码转换工具？",
    "faq1A": "OmniKit 提供 7 个编码转换工具：Base64 编解码、URL 编解码、JSON/CSV 转换器、CSV/Markdown 表格转换、数字进制转换、JSON/YAML 转换和存储单位换算。",
    "faq2Q": "编码转换是否准确？",
    "faq2A": "是的。所有转换严格遵循相应的 RFC 规范（Base64 遵循 RFC 4648，URL 编码遵循 RFC 3986），输出符合标准。"
  },
  "security": {
    "title": "安全加密工具 - JWT、哈希、加密 | OmniKit",
    "shortTitle": "安全加密",
    "description": "面向开发者的免费在线安全加密工具。调试 JWT、生成哈希、创建安全密码、生成 SSH 密钥、加密文本、计算文件校验和。100% 浏览器端处理。",
    "intro": "6 个安全加密工具。调试和验证 JWT 令牌、生成 MD5/SHA 哈希、创建加密安全密码、生成 SSH 密钥对、使用 AES/DES/RC4 加密解密文本、计算文件校验和。所有加密操作完全在浏览器中运行，数据不会离开你的设备。",
    "faq1Q": "安全工具处理敏感数据安全吗？",
    "faq1A": "安全。所有安全工具使用 JavaScript 加密库在浏览器端 100% 本地运行，不会将任何数据传输到服务器。你可以通过查看开源代码来验证。",
    "faq2Q": "支持哪些加密算法？",
    "faq2A": "加密解密工具支持 AES-256、DES、Triple DES、Rabbit、RC4 和 RC4Drop。哈希生成器支持 MD5、SHA-1、SHA-224、SHA-256、SHA-384、SHA-512、SHA3、Keccak 和 RIPEMD-160。"
  },
  "generators": {
    "title": "生成器工具 - UUID、Cron、时间戳、二维码 | OmniKit",
    "shortTitle": "生成器",
    "description": "面向开发者的免费在线生成器工具。生成 UUID、构建 Cron 表达式、转换 Unix 时间戳、创建二维码。所有工具完全在浏览器中运行。",
    "intro": "4 个生成器工具，帮助创建常用的开发者资源。生成 UUID v4/v7 标识符、构建和解析 Cron 表达式并预览下次执行时间、转换 Unix 时间戳、生成带样式的二维码。所有操作均在浏览器本地完成。",
    "faq1Q": "OmniKit 可以生成什么？",
    "faq1A": "OmniKit 提供 4 个生成器工具：UUID 生成器（v4/v7）、Cron 表达式生成器、Unix 时间戳转换器和带自定义样式的二维码生成器。",
    "faq2Q": "生成的 UUID 是否唯一？",
    "faq2A": "是的。UUID v4 使用加密安全的随机数，UUID v7 结合时间戳和随机性，两者均符合 RFC 4122/9562 标准。"
  },
  "visual": {
    "title": "视觉媒体工具 - 颜色转换器、图片压缩 | OmniKit",
    "shortTitle": "视觉媒体",
    "description": "面向开发者的免费在线视觉媒体工具。HEX/RGB/HSL/OKLCH 颜色转换、图片调色板提取、图片压缩转换。100% 浏览器端处理。",
    "intro": "2 个视觉媒体工具。颜色转换器支持 7 种色彩空间（HEX、RGB、HSL、HSV、CMYK、LAB、OKLCH），具备取色器、图片调色板提取、WCAG 对比度检查和色盲模拟功能。图片压缩器支持 PNG、JPG、WebP 格式转换和尺寸调整。所有处理均在本地运行。",
    "faq1Q": "支持哪些色彩空间？",
    "faq1A": "颜色转换器支持 HEX、RGB、HSL、HSV、CMYK、LAB 和 OKLCH，支持双向转换和可视化对比。",
    "faq2Q": "图片压缩有文件大小限制吗？",
    "faq2A": "没有。图片压缩完全在浏览器中使用 Canvas API 处理，因此没有服务端文件大小限制。非常大的图片处理可能会稍慢。"
  },
  "reference": {
    "title": "参考查询工具 - HTTP 状态码、ASCII、HTML 实体 | OmniKit",
    "shortTitle": "参考查询",
    "description": "面向开发者的免费在线参考查询工具。HTTP 状态码、ASCII 表、HTML 实体、SQLite 浏览器、HTTP 客户端。所有工具完全在浏览器中运行。",
    "intro": "5 个参考查询工具，方便开发者快速查找。浏览 HTTP 状态码及 RFC 参考、查询 ASCII 字符编码、编码 HTML 实体、使用 SQL 编辑器浏览 SQLite 数据库、使用 HTTP 客户端测试 REST API。所有工具免费且完全在浏览器中运行。",
    "faq1Q": "OmniKit 提供哪些参考工具？",
    "faq1A": "OmniKit 提供 5 个参考工具：HTTP 状态码参考、HTTP 客户端（API 测试）、SQLite 数据库浏览器、ASCII 表和 HTML 实体编码器。",
    "faq2Q": "可以在数据库浏览器中执行 SQL 查询吗？",
    "faq2A": "可以。SQLite 浏览器支持只读 SQL 查询，具备自动补全、查询历史、分页功能，并支持导出为 CSV 或 JSON——全部通过 WebAssembly 在浏览器本地运行。"
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add public/locales/zh-CN/categories.json
git commit -m "feat(seo): add Chinese category page content"
```

---

### Task 19: zh-CN locale files — text category (8 tools)

**Files:**

- Modify: `public/locales/zh-CN/json.json`
- Modify: `public/locales/zh-CN/regex.json`
- Modify: `public/locales/zh-CN/diff.json`
- Modify: `public/locales/zh-CN/markdown.json`
- Modify: `public/locales/zh-CN/textcase.json`
- Modify: `public/locales/zh-CN/extractor.json`
- Modify: `public/locales/zh-CN/wordcounter.json`
- Modify: `public/locales/zh-CN/deduplines.json`

For each tool, add `"aeoDefinition"` inside the `"descriptions"` object and modify link paragraphs with `[text](/path)` syntax (paths are always English tool routes).

- [ ] **Step 1: json.json — Add aeoDefinition + modify whatIsP1, useCasesP2**

```json
"aeoDefinition": "JSON 格式化工具是一个免费的在线 JSON 处理器，可以在浏览器中即时格式化、验证和压缩 JSON 数据。支持 JSON5、可配置缩进和键排序。所有操作均在本地完成，不会上传任何数据。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "JSON（JavaScript Object Notation）是网络通用数据交换格式。API 返回 JSON，配置文件使用 JSON，所有现代编程语言都可以解析 JSON。配合 [YAML 转换器](/yaml) 或 [CSV 转换器](/csv) 进行跨格式转换。格式化（美化打印）将压缩的、难以阅读的 JSON 添加缩进和换行，使结构一目了然。",
```

Modify `useCasesP2`:

```json
"useCasesP2": "压缩配置文件：部署到生产前压缩 JSON 以减小文件大小。使用 [文本对比](/diff) 验证压缩后的输出与原始内容一致。",
```

- [ ] **Step 2: regex.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "正则测试器是一个免费的在线正则表达式测试工具，支持实时匹配高亮、捕获组检查和替换预览。内置模式库和逐令牌解释。完全在浏览器中运行。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "正则表达式测试器是一个交互式工具，可以实时构建和测试正则表达式模式。配合 [JSON 格式化](/json) 进行数据验证，或使用 [大小写转换](/textcase) 进行批量文本转换。",
```

- [ ] **Step 3: diff.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "文本对比工具是一个免费的在线文本比较工具，支持行级和词级精确对比。并排或内联视图，变更高亮显示。完全在浏览器中运行，不会发送任何数据到服务器。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "Diff 是两段文本之间的结构化比较，突出显示新增内容、删除内容和未变内容。开发者每天都在使用它来审查代码变更、审计配置漂移，以及比较 [JSON](/json) 或 [CSV](/csv) 文件。",
```

Modify `useCasesP1`:

```json
"useCasesP1": "比较两个配置文件（[JSON](/json)、[YAML](/yaml)、.env），发现环境间的差异。",
```

- [ ] **Step 4: markdown.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "Markdown 编辑器是一个免费的在线 Markdown 编写和预览工具，支持 GitHub 风格 Markdown。包含语法高亮、实时预览和 PDF/PNG 导出。完全在浏览器中运行。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "Markdown 是一种轻量级标记语言，可以用简单、可读的语法格式化纯文本。它是 README、文档和笔记的事实标准——易于编写，易于 [对比](/diff)，在各平台渲染效果出色。",
```

- [ ] **Step 5: textcase.json — Add aeoDefinition**

```json
"aeoDefinition": "大小写转换工具是一个免费的在线文本转换器，支持 camelCase、PascalCase、snake_case、kebab-case、CONSTANT_CASE 等格式。即时转换，实时预览。数据不会离开你的浏览器。",
```

- [ ] **Step 6: extractor.json — Add aeoDefinition**

```json
"aeoDefinition": "邮箱/URL 提取器是一个免费的在线文本提取工具，可以从任意文本中即时提取邮箱、URL 和电话号码。支持多种格式和批量处理。完全在浏览器中运行。",
```

- [ ] **Step 7: wordcounter.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "字数统计工具是一个免费的在线文本分析工具，提供实时字数、字符数、句子数、段落数和阅读时间估算。支持中日韩文本。不会发送任何数据到服务器。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "字数统计工具是一个免费的在线文本分析工具，提供实时字数、字符数、句子数、段落数和阅读时间估算。配合 [大小写转换](/textcase) 进行批量文本处理，或使用 [去重工具](/deduplines) 清理列表。",
```

- [ ] **Step 8: deduplines.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "去重工具是一个免费的在线文本去重工具，在保留顺序的同时移除重复行。支持区分大小写、空白修剪和空行移除。完全在浏览器中运行。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "行去重是从文本中移除重复行，仅保留每个唯一行的首次出现。原始顺序保持不变。使用 [邮箱提取器](/extractor) 提取唯一地址，或用 [字数统计](/wordcounter) 分析清理后的结果。",
```

- [ ] **Step 9: Commit**

```bash
git add public/locales/zh-CN/json.json public/locales/zh-CN/regex.json public/locales/zh-CN/diff.json public/locales/zh-CN/markdown.json public/locales/zh-CN/textcase.json public/locales/zh-CN/extractor.json public/locales/zh-CN/wordcounter.json public/locales/zh-CN/deduplines.json
git commit -m "feat(seo): add zh-CN aeoDefinition + inline links for text category tools"
```

---

### Task 20: zh-CN locale files — encoding category (7 tools)

**Files:**

- Modify: `public/locales/zh-CN/base64.json`
- Modify: `public/locales/zh-CN/urlencoder.json`
- Modify: `public/locales/zh-CN/csv.json`
- Modify: `public/locales/zh-CN/csv-md.json`
- Modify: `public/locales/zh-CN/numbase.json`
- Modify: `public/locales/zh-CN/yaml.json`
- Modify: `public/locales/zh-CN/storageunit.json`

- [ ] **Step 1: base64.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "Base64 编解码工具是一个免费的在线 Base64 字符串编码和解码工具。支持 Basic 认证头、多种字符集和实时转换。所有处理均在浏览器中完成。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "Base64 编码是将数据（通常是二进制）转换为 ASCII 字符集的一种方式。需要注意的是，Base64 不是加密或压缩技术。使用 [哈希生成器](/hashing) 进行加密处理，或使用 [URL 编码](/urlencoder) 生成 URL 安全字符串。",
```

Modify `useCasesP1`:

```json
"useCasesP1": "你还可以使用 Base64 以兼容 HTML、JavaScript 和 CSS 的方式表示二进制数据。[加密解密](/cipher) 工具使用 Base64 作为加密文本的默认输出格式。",
```

- [ ] **Step 2: urlencoder.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "URL 编解码工具是一个免费的在线 URL 字符串编码和解码工具。支持组件模式、完整 URL 模式和表单模式。完全在浏览器中运行，不会发送任何数据到服务器。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "URL 编码（又称百分号编码）是 RFC 3986 定义的在 URL 中承载数据的机制。在 URL 中具有保留含义的字符（如 ? / # & =）或超出 ASCII 可打印范围的字符被重写为一个或多个 %XX 三元组。另请参阅 [Base64](/base64) 用于二进制到文本的编码。",
```

- [ ] **Step 3: csv.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "CSV 转换器是一个免费的在线 JSON 与 CSV 格式互转工具，支持实时预览。双向转换，正确处理转义和 Unicode。数据不会离开你的浏览器。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "CSV（逗号分隔值）是一种用于存储表格数据的纯文本格式。每行代表一条记录，字段用逗号分隔。它是电子表格、数据库和应用程序之间数据交换最常用的格式。使用 [JSON 格式化](/json) 检查源数据，或使用 [YAML 转换器](/yaml) 输出 YAML。",
```

Modify `useCasesP1`:

```json
"useCasesP1": "在电子表格和 Web API 之间导入/导出数据，将 [JSON](/json) API 响应转换为 CSV 进行分析，使用 [数据库浏览器](/dbviewer) 准备数据库批量导入数据。",
```

- [ ] **Step 4: csv-md.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "CSV ↔ Markdown 表格转换器是一个免费的在线 CSV 和 Markdown 表格格式互转工具。实时双向转换。完全在浏览器中运行。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "在 CSV（逗号分隔值）和 Markdown 表格格式之间转换。CSV 是电子表格数据交换的标准格式，Markdown 表格常用于文档、README 文件和 GitHub Issues。使用 [CSV 转换器](/csv) 进行 JSON↔CSV 转换，或使用 [Markdown 编辑器](/markdown) 编辑 Markdown 文件。",
```

- [ ] **Step 5: numbase.json — Add aeoDefinition + modify whatIs**

```json
"aeoDefinition": "进制转换器是一个免费的在线数字进制转换工具，支持二进制、八进制、十进制和十六进制互转。支持 8/16/32/64 位补码和可视化位编辑器。完全在浏览器中运行。",
```

Modify `whatIs`:

```json
"whatIs": "此工具在二进制（基数 2）、八进制（基数 8）、十进制（基数 10）和十六进制（基数 16）之间实时转换数字。还支持 8/16/32/64 位补码表示及可视化位编辑器。相关工具：[颜色转换器](/color) 用于十六进制颜色代码，[存储单位换算](/storageunit) 用于字节计算。",
```

- [ ] **Step 6: yaml.json — Add aeoDefinition + modify whatIsP1, useCasesP1**

```json
"aeoDefinition": "YAML 转换器是一个免费的在线 JSON 与 YAML 格式互转工具。支持 YAML 1.2、多文档、锚点和别名。所有处理均在浏览器中完成。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "YAML（YAML 不是标记语言）是一种人类可读的数据序列化格式，常用于配置文件、数据交换和结构化内容。它使用缩进表示结构，比 [JSON](/json) 更易读。",
```

Modify `useCasesP1`:

```json
"useCasesP1": "配置文件（Docker Compose、Kubernetes、CI/CD 流水线）、API 定义（OpenAPI/Swagger）、数据序列化、日志文件格式和跨语言数据交换。转换为 [CSV](/csv) 用于电子表格导入，或使用 [文本对比](/diff) 比较版本差异。",
```

- [ ] **Step 7: storageunit.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "存储单位换算工具是一个免费的在线字节、KB、MB、GB、TB、PB 换算工具。支持 SI（十进制）和 IEC（二进制）标准。完全在浏览器中运行。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "存储单位换算工具可以即时转换数字存储单位——字节、KB、MB、GB、TB、PB——支持十进制（SI）和二进制（IEC）标准。另请参阅 [进制转换器](/numbase) 进行二进制/十六进制转换，或使用 [文件校验和](/checksum) 验证文件大小。",
```

- [ ] **Step 8: Commit**

```bash
git add public/locales/zh-CN/base64.json public/locales/zh-CN/urlencoder.json public/locales/zh-CN/csv.json public/locales/zh-CN/csv-md.json public/locales/zh-CN/numbase.json public/locales/zh-CN/yaml.json public/locales/zh-CN/storageunit.json
git commit -m "feat(seo): add zh-CN aeoDefinition + inline links for encoding category tools"
```

---

### Task 21: zh-CN locale files — security category (6 tools)

**Files:**

- Modify: `public/locales/zh-CN/jwt.json`
- Modify: `public/locales/zh-CN/hashing.json`
- Modify: `public/locales/zh-CN/password.json`
- Modify: `public/locales/zh-CN/sshkey.json`
- Modify: `public/locales/zh-CN/cipher.json`
- Modify: `public/locales/zh-CN/checksum.json`

- [ ] **Step 1: jwt.json — Add aeoDefinition + modify whatIs**

```json
"aeoDefinition": "JWT 调试器是一个免费的在线 JSON Web Token 编码、解码和验证工具。支持 HS256、RS256、ES256 等多种算法。所有处理均在浏览器中完成，不会将令牌发送到任何服务器。",
```

Modify `whatIs`:

```json
"whatIs": "JSON Web Token (JWT) 是一种开放标准（RFC 7519），用于在各方之间以 JSON 对象的形式安全传输信息。JWT 常用于 Web 应用程序的身份验证和授权。在此调试令牌，然后使用 [Base64](/base64) 解码验证载荷，或使用 [哈希生成器](/hashing) 检查密钥完整性。",
```

- [ ] **Step 2: hashing.json — Add aeoDefinition**

```json
"aeoDefinition": "哈希生成器是一个免费的在线 MD5、SHA-1、SHA-256、SHA-512、SHA3、Keccak 和 RIPEMD-160 哈希生成工具。支持 HMAC 和文件哈希。不会发送任何数据到服务器。",
```

- [ ] **Step 3: password.json — Add aeoDefinition**

```json
"aeoDefinition": "密码生成器是一个免费的在线加密安全密码生成工具。支持随机密码、易记密码和 PIN 码生成，内置强度检测。完全在浏览器中运行。",
```

- [ ] **Step 4: sshkey.json — Add aeoDefinition**

```json
"aeoDefinition": "SSH 密钥生成器是一个免费的在线 RSA 和 Ed25519 SSH 密钥对生成工具，完全在浏览器中运行。支持密码短语加密和指纹显示。不会发送任何数据到服务器。",
```

- [ ] **Step 5: cipher.json — Add aeoDefinition**

```json
"aeoDefinition": "加密解密工具是一个免费的在线文本加密解密工具，支持 AES、DES、Triple DES、Rabbit、RC4 和 RC4Drop。所有处理均在浏览器中完成，不会发送任何数据到服务器。",
```

- [ ] **Step 6: checksum.json — Add aeoDefinition**

```json
"aeoDefinition": "文件校验和工具是一个免费的在线 MD5、SHA-1、SHA-256、SHA-512 校验和计算工具。支持无限文件大小，100% 浏览器端处理。",
```

- [ ] **Step 7: Commit**

```bash
git add public/locales/zh-CN/jwt.json public/locales/zh-CN/hashing.json public/locales/zh-CN/password.json public/locales/zh-CN/sshkey.json public/locales/zh-CN/cipher.json public/locales/zh-CN/checksum.json
git commit -m "feat(seo): add zh-CN aeoDefinition for security category tools"
```

---

### Task 22: zh-CN locale files — generators category (4 tools)

**Files:**

- Modify: `public/locales/zh-CN/uuid.json`
- Modify: `public/locales/zh-CN/cron.json`
- Modify: `public/locales/zh-CN/unixtime.json`
- Modify: `public/locales/zh-CN/qrcode.json`

- [ ] **Step 1: uuid.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "UUID 生成器是一个免费的在线 UUID v4（随机）和 v7（时间排序）批量生成工具。符合 RFC 4122/9562 标准。完全在浏览器中运行。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "UUID（通用唯一标识符）是 RFC 4122 和 RFC 9562 定义的 128 位标识符标准。此工具批量生成 UUID v4（随机）和 v7（时间排序）。将生成的 ID 用作 [密码生成器](/password) 的种子，编码到 [二维码](/qrcode) 中，或使用 [哈希生成器](/hashing) 验证唯一性。",
```

- [ ] **Step 2: cron.json — Add aeoDefinition + modify whatIs**

```json
"aeoDefinition": "Cron 表达式生成器是一个免费的在线 Cron 表达式构建和解析工具。支持 Standard、Spring 和 Quartz 模式，提供人类可读的解释和下次执行时间预览。数据不会离开你的浏览器。",
```

Modify `whatIs`:

```json
"whatIs": "Cron 是类 Unix 操作系统中基于时间的作业调度器。Cron 表达式是由五个或六个字段组成的字符串，定义了作业应何时运行。此工具支持 Standard、Spring 和 Quartz 方言。配合 [Unix 时间戳](/unixtime) 转换器进行时间计算。",
```

- [ ] **Step 3: unixtime.json — Add aeoDefinition**

```json
"aeoDefinition": "Unix 时间戳转换器是一个免费的在线 Unix 时间戳与可读日期互转工具。支持秒和毫秒，带实时时钟。完全在浏览器中运行。",
```

- [ ] **Step 4: qrcode.json — Add aeoDefinition**

```json
"aeoDefinition": "二维码生成器是一个免费的在线二维码生成工具，支持文本、URL、WiFi、vCard 等内容。支持自定义颜色、Logo 和点样式，可导出 SVG/PNG。不会发送任何数据到服务器。",
```

- [ ] **Step 5: Commit**

```bash
git add public/locales/zh-CN/uuid.json public/locales/zh-CN/cron.json public/locales/zh-CN/unixtime.json public/locales/zh-CN/qrcode.json
git commit -m "feat(seo): add zh-CN aeoDefinition + inline links for generator category tools"
```

---

### Task 23: zh-CN locale files — visual category (2 tools)

**Files:**

- Modify: `public/locales/zh-CN/color.json`
- Modify: `public/locales/zh-CN/image.json`

- [ ] **Step 1: color.json — Add aeoDefinition + modify description.introP1**

```json
"aeoDefinition": "颜色转换器是一个免费的在线 HEX、RGB、HSL、OKLCH 等色彩空间互转工具。包含取色器、图片调色板提取、WCAG 对比度检查和色盲模拟。完全在浏览器中运行。",
```

Modify `description.introP1`:

```json
"introP1": "此颜色工具支持 7 种色彩空间互转、生成和谐色、检查对比度（WCAG 和 APCA）、模拟色盲以及从图片提取调色板。参见 [进制转换器](/numbase) 进行十六进制计算，或使用 [图片压缩](/image) 进行图片处理。",
```

- [ ] **Step 2: image.json — Add aeoDefinition + modify descriptions.p1**

```json
"aeoDefinition": "图片压缩工具是一个免费的在线图片压缩、调整大小和格式转换工具，支持 PNG、JPG、WebP 格式。所有处理均在浏览器本地运行。",
```

Modify `descriptions.p1`:

```json
"p1": "在浏览器中直接压缩、调整大小和转换 PNG、JPG、WebP 格式的图片。使用 [颜色转换器](/color) 提取主色调，或使用 [文件校验和](/checksum) 验证图片完整性。",
```

- [ ] **Step 3: Commit**

```bash
git add public/locales/zh-CN/color.json public/locales/zh-CN/image.json
git commit -m "feat(seo): add zh-CN aeoDefinition + inline links for visual category tools"
```

---

### Task 24: zh-CN locale files — reference category (5 tools)

**Files:**

- Modify: `public/locales/zh-CN/httpstatus.json`
- Modify: `public/locales/zh-CN/httpclient.json`
- Modify: `public/locales/zh-CN/dbviewer.json`
- Modify: `public/locales/zh-CN/ascii.json`
- Modify: `public/locales/zh-CN/htmlcode.json`

- [ ] **Step 1: httpstatus.json — Add aeoDefinition + modify description.text**

```json
"aeoDefinition": "HTTP 状态码参考是一个免费的在线 HTTP 状态码完整参考，包含描述、分类和 RFC 链接。覆盖 IANA 官方代码和常见非官方代码。",
```

Modify `description.text`:

```json
"text": "完整的 HTTP 状态码参考，包含描述、分类和 RFC 规范链接。覆盖所有 IANA 注册代码及常见非官方代码。使用 [HTTP 客户端](/httpclient) 测试 API，或使用 [URL 编码](/urlencoder) 编码 URL。",
```

- [ ] **Step 2: httpclient.json — Add aeoDefinition + modify description.text**

```json
"aeoDefinition": "HTTP 客户端是一个免费的在线 HTTP 请求发送和 REST API 测试工具，完全在浏览器中运行。支持 GET、POST、PUT、DELETE，包含请求头、请求体和认证。数据不会通过任何服务器。",
```

Modify `description.text`:

```json
"text": "在浏览器中直接发送 HTTP 请求和测试 REST API。支持所有常用方法，包含请求头、请求体和身份验证。在 [HTTP 状态码](/httpstatus) 参考中查找状态码含义，或使用 [URL 编码](/urlencoder) 编码查询参数。",
```

- [ ] **Step 3: dbviewer.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "SQLite 浏览器是一个免费的在线 SQLite 数据库文件查看工具。支持 SQL 查询、自动补全、表浏览，可导出为 CSV 或 JSON。数据不会离开你的设备。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "SQLite 浏览器让你可以直接在浏览器中打开和浏览 SQLite 数据库文件——无需上传到服务器。执行 SQL 查询、浏览表结构，并将数据导出为 [CSV](/csv) 或 [JSON](/json)。基于 sql.js（WebAssembly）。",
```

- [ ] **Step 4: ascii.json — Add aeoDefinition + modify whatIsP1**

```json
"aeoDefinition": "ASCII 表是一个免费的在线 ASCII 字符参考工具，包含十六进制、八进制、十进制和 HTML 代码。支持搜索、过滤和转换。完全在浏览器中运行。",
```

Modify `whatIsP1`:

```json
"whatIsP1": "ASCII（美国信息交换标准代码）是一种字符编码标准，为字母、数字、标点符号和控制代码分配数值。它涵盖 0 到 127 的 128 个字符。参见 [HTML 实体](/htmlcode) 了解网页安全字符，或使用 [进制转换器](/numbase) 进行十六进制/二进制转换。",
```

- [ ] **Step 5: htmlcode.json — Add aeoDefinition + modify description.p1**

```json
"aeoDefinition": "HTML 实体编码器是一个免费的在线 HTML 特殊字符和实体编码解码工具。包含完整的数字和命名实体参考。完全在浏览器中运行。",
```

Modify `description.p1`:

```json
"p1": "HTML 实体是用于表示 HTML 中保留字符的特殊代码。例如，&lt; 表示 <，&amp; 表示 &，&quot; 表示 \"。参见 [ASCII 表](/ascii) 了解字符编码，或使用 [HTTP 状态码](/httpstatus) 了解 Web 响应参考。",
```

- [ ] **Step 6: Commit**

```bash
git add public/locales/zh-CN/httpstatus.json public/locales/zh-CN/httpclient.json public/locales/zh-CN/dbviewer.json public/locales/zh-CN/ascii.json public/locales/zh-CN/htmlcode.json
git commit -m "feat(seo): add zh-CN aeoDefinition + inline links for reference category tools"
```

---

### Task 25: Final verification

- [ ] **Step 1: Run all tests**

Run: `npm run test`
Expected: All tests pass

- [ ] **Step 2: Run lint**

Run: `npm run lint`
Expected: No errors

- [ ] **Step 3: Build check**

Run: `npx next build`
Expected: Build succeeds

- [ ] **Step 4: Manual smoke test**

Start dev server and verify:

1. Visit `/json` — AEO definition paragraph visible at top of Description, category badge "Text Processing" links to `/text-processing`
2. Visit `/text-processing` — Category page renders with 8 tool cards, FAQ section visible
3. Visit `/json` — inline links in whatIsP1 are clickable and link to `/csv` and `/yaml`
4. View page source on `/json` — BreadcrumbList schema has 3 levels (OmniKit > Text Processing > JSON)
5. Visit `/zh-CN/json` — Chinese AEO definition visible, category badge links to `/zh-CN/text-processing`
6. Visit `/sitemap.xml` — 6 category pages present
7. Homepage — Category section headers are clickable links
